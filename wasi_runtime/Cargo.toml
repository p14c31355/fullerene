[package]
name = "wasi_runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmi = { version = "0.33", default-features = false }
carrier = { path = "../carrier" }

[lib]
name = "wasi_runtime"
path = "src/lib.rs"
crate-type = ["lib"]
