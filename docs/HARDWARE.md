# Real Hardware Compatibility

For a compact, context-efficient status of the active Bramble investigation,
start with [CONTEXT_STATUS.md](CONTEXT_STATUS.md). The Bramble section below
is a cross-platform summary; the complete AArch64 ledger is in
[HARDWARE_aarch64.md](HARDWARE_aarch64.md).

## ESP32-2432S028 / Sparkle IoT XH-32S (bring-up)

This ESP32-class board is a new Xtensa platform profile, not an ESP-IDF
application. The Xtensa port is split into each Fullerene crate's
`src/arch/xtensa/esp32/` tree; there is no separate `fullerene-esp32` crate.
Flasks is the build/flash/monitor task runner.

The current port initializes UART, disables both watchdog mechanisms during
early boot, and reaches a stable cooperative scheduler. The ESP32 ELF and image
generation path builds, and `esptool image-info` accepts the image. This is not
evidence of a working desktop. The following remain bring-up items and may fail
explicitly rather than pretending success:

- timer preemption and exception-frame context switching
- physical confirmation of SPI LCD protocol/backlight behaviour
- I2C resistive-touch controller and calibration
- SDMMC host and FAT mounting
- persistent settings and interactive shell integration

Board pin defaults currently used by the profile are unverified against traces:

| Device | Pins |
| --- | --- |
| LCD SPI | SCLK 14, MOSI 13, DC 2, CS 15, RST 12, backlight 21 |
| Touch SPI | CLK 25, MOSI 32, MISO 39, CS 33, PENIRQ 36 (external pull-up required) |
| SDMMC | CLK 18, CMD 23, DATA0 19 |

Do not treat these values as hardware confirmation. Probe traces, chip IDs, and
runtime responses before relying on them; update this table only with verified
evidence.

## InsydeH2O Firmware (June 2026)

Running on real hardware with InsydeH2O UEFI firmware required three fixes:

1. **Do not call `SetMode()`**: InsydeH2O's GOP implementation changes `frame_buffer_base` and/or invalidates `pixels_per_scan_line` after `SetMode()`, causing "backlight only" (no display output). The bootloader now uses the current mode as-is.

2. **BGR/RGB byte-order in `rgb888_to_pixel_format()`**: The color conversion function had its byte-order arguments reversed for BGR vs RGB pixel formats. For BGR hardware (common on Intel GOP), `rgb_pixel(r,g,b)` produces the correct LE memory layout `[b,g,r,0]` which BGR interprets as B=b, G=g, R=r. The fix corrects the mapping: BGR/PixelBitMask formats use `rgb_pixel(r,g,b)` while RGB formats use `rgb_pixel(b,g,r)`.

3. **Skip `safe_map_page` WC remap on real hardware**: The kernel's `safe_map_page` (via `map_page_4k_l1`) attempts to split the boot-phase 2MB/1GB huge-page WB mapping into 4KB WC pages for the framebuffer. On InsydeH2O this operation breaks the mapping entirely, making the framebuffer inaccessible. The fix relies on the existing boot-phase huge-page identity mapping (WB via PAT/MTRR), which is already functional and confirmed working via direct `write_volatile` tests.

The same rule applies to PCI device MMIO. `map_mmio_region` first verifies the
start and end of the requested higher-half direct mapping and reuses it when it
already resolves to the BAR. This keeps USB xHCI and the RTS5249 reader from
splitting a working boot huge page immediately before their first register
access. Only an actually missing or non-direct mapping is created dynamically.

## Intel Wildcat Point-LP USB (8086:9cb1 / 8086:9ca6)

The target machine exposes an xHCI controller at `00:14.0` and an EHCI
companion at `00:1d.0`. Before the first xHCI BAR0 load, Fullerene now:

1. moves the endpoint to D0;
2. enables the Intel USB3 terminations (`USB3_PSSEN`, config `0xd8`), then
   routes the firmware-declared USB2 ports (`XUSB2PR`, config `0xd0`) to xHCI;
3. disables standard ASPM and enables PCI memory decoding/bus mastering;
4. verifies the existing BAR0 direct mapping and reads the capability header as
   a 32-bit register;
5. performs the xHCI BIOS/OS ownership handoff, disables legacy SMIs, and
   waits for `USBSTS.CNR` to clear before operational-register access.

Boot registers the USB service without touching either controller BAR. The
sequence above starts only on the explicit `usb_rescan` command. Desktop and
File Manager polling never activates a deferred controller, so an uncompleted
PCIe read cannot block boot, rendering, or input dispatch. xHCI is initialized
before its companion; after Intel routing is confirmed and xHCI is active,
Fullerene does not access the unsupported EHCI companion. EHCI-only systems
still use the EHCI path, which is initialized once and never restarted by
polling.

The runtime interrupter register set begins at `RTSOFF + 0x20` (after
`MFINDEX`); using `RTSOFF` directly writes the wrong registers. Capability,
operational, runtime, doorbell, and extended-capability offsets are rejected if
they exceed the mapped BAR window.

`core::ptr::read_volatile`, an MMIO wrapper, inline assembly, and an external
xHCI crate all ultimately issue the same non-posted CPU load. None can impose a
software timeout on a PCIe transaction that never completes. The removed
`detect_abort_read_u32` helper only classified an all-ones value *after* a load
completed and therefore did not prevent a hang. Fullerene instead performs
configuration-space preflight before the first MMIO access; later watchdog
recovery remains a platform mechanism, not a replacement read primitive.

Real-hardware validation is still required for the complete controller reset,
port enumeration, and mass-storage path on this machine.

## AHCI SATA storage and rescan behavior

AHCI devices are matched by PCI class `0x01`, subclass `0x06` rather than by a
vendor/device allowlist. The kernel enables PCI memory decoding, maps BAR5 as
the HBA register window, performs an HBA reset, reads `GHC` and `PI`, and then
probes each implemented port. `PI` is a bit mask: a message such as
`ahci0 (1 ports)` means that one HBA port is implemented, not that one disk was
identified.

For each implemented port, the driver waits up to one second for
`PxSSTS.DET == 0x3` (`device present, PHY established`). It then stops the
command engine, allocates the command list/FIS/command table/DMA buffer,
starts FIS reception and command processing, and sends ATA `IDENTIFY DEVICE`
(`0xEC`). A port is published only after IDENTIFY reports a supported sector
size and a non-zero sector count. Successful disks are registered as
`/dev/sataNpN`; the port number is the AHCI port number, so a disk on HBA port
3 may appear as `/dev/sata0p3`.

`ahci_init` is safe to call repeatedly. If the controller already exists, it
re-probes implemented ports that have not yet completed IDENTIFY and then
refreshes the kernel block-device registry. This covers SATA links that become
ready after boot and prevents a controller-only initialization from leaving a
stale empty `/dev` view. The installer also refreshes its target list when
moving from Welcome to disk selection.

The following log sequence distinguishes controller readiness from disk
readiness:

```text
AHCI: HBA BAR5=... GHC=... PI=...
AHCI port 3: ATA disk (... bytes/sector, ... sectors)
AHCI: registered /dev/sata0p3 (... bytes/sector, ... sectors)
AHCI init: ahci0 ready; 1 ATA disk(s) registered
```

If the final port message is `no PHY ... (SSTS=..., SERR=...)`, the HBA was
found but no SATA link reached the PHY-ready state. In that case no AHCI block
device is registered and the installer correctly reports that no target is
available. The BAR5, `GHC`, `PI`, `PxSSTS`, and `PxSERR` values should be
captured before investigating the SATA cable/connector, firmware storage mode,
or a platform-specific link-reset requirement.

## Intel iwlwifi 7265-family PCI probe

The supported Intel IDs are `8086:095b`, `8086:095a`, `8086:08b1`, and
`8086:08b2`. Wi-Fi initialization is deferred to Solvent's service tick, so a
driver probe cannot block the kernel's boot sequence indefinitely. The phases
are PCI discovery, MMIO setup, DMA allocation, firmware upload/alive polling,
and post-alive initialization commands.

The PCI probe now preserves the firmware-assigned BAR0. It uses
`read_bar_info()` and maps an 8 KiB register window; it does not use
`get_bar_info()`, whose all-ones BAR size probe is unsafe for a live endpoint on
the affected hardware. The upstream bridge is taken from the same scan rather
than triggering a second full bus walk. PCI config-lock acquisition is bounded
so an abandoned transaction cannot permanently spin the CPU.

The affected real machine has now been observed to advance past
`step: start pci_probe` and reach `step: mmio_poll_mac`, confirming that the
non-destructive PCI/BAR probe fixes the original hang. The post-reset CSR
sequence sets both `MAC_ACCESS_REQ` and `INIT_DONE`, as required before
`MAC_CLOCK_READY` can become set; the recovery path uses the same bits. The
diagnostic marker `mmio_mac_clock_wait` means the CSR read completed but the
clock was not ready, while `mmio_read_mac` confirms that this stage passed.
The target has now advanced beyond this stage to firmware upload, but the
outer runtime timeout previously stopped it while waiting for firmware alive.
The runtime loader now selects only the `SEC_RT` image sections, skips the
firmware section separator, reports each loaded section through
`FH_UCODE_LOAD_STATUS`, follows the GP1 mailbox clear protocol, detects
7265D using CSR `HW_REV` before selecting firmware, and gives the bounded
firmware-candidate sequence enough time to finish. Physical
validation of this follow-up build is still required, so the support level
remains Alpha.

### Klog Live validation without a serial console

On the real machine, open the network menu once to request deferred iwlwifi
initialization and the first scan. Open `KLog Live` from the same desktop and
keep it visible while the scan runs. The three goal checks have these log
markers:

- Startup audio: `Sound: startup PCM playback complete` and, for HDA,
  `HDA: controller ready`.
- BusyBox: `BUSYBOX-DIAG: launch complete`, followed by
  `BUSYBOX-DIAG: terminal owner exited ... code=0 terminal closed`; the
  BusyBox terminal must return to the Fullerene shell after `exit`.
- Wi-Fi: `iwlwifi: AP FOUND ssid=...`, followed by
  `iwlwifi: scan complete (N APs found)`. When a known test AP is available,
  require `N > 0`; otherwise successful firmware readiness and scan completion
  are sufficient.

For a real-hardware USB rescan, open `KLog Live` before running `usb_rescan`.
The command returns after queue acceptance; controller activation and
enumeration continue in the scheduler-owned device phase. The stable
`[USB-RESCAN]` markers identify the last completed boundary:

```text
[USB-RESCAN] queue accepted
[USB-RESCAN] activate: USBContext::enable begin
[USB-RESCAN] activate: USBContext::enable returned
[USB-RESCAN] poll: controller poll begin
[USB-RESCAN] poll: controller poll returned
```

If the last marker is `queue accepted`, the request has not reached the device
phase yet. If it is `USBContext::enable begin`, the PCI/xHCI activation path is
stuck. If it is `poll: controller poll begin`, root-port or device enumeration
is in progress. These markers are emitted to the kernel log and taskbar ring;
they are not synchronously mirrored to the serial stream. Sealant still bounds
the MMIO region and permissions, while the NMI watchdog is the mechanism that
can recover from a non-posted PCIe read that never completes; Sealant alone
cannot cancel such a hardware transaction.

These markers are in the persistent kernel log, so serial capture is not
required. A scan that reaches `scan complete (0 APs found)` is valid when no
known test AP is expected; firmware initialization/readiness and scan
completion are the health criteria in that case.

The Realtek RTS5249 reader (`10ec:5249`) is matched by vendor/device identity,
because PCI class `0xff` is a real vendor-specific class rather than a driver
wildcard. Boot registers the reader without accessing its device registers.
The explicit `sd_rescan` command is the first BAR0 MMIO boundary; a successfully
initialized SDXC then appears dynamically as `/dev/sd0` without being mounted.
Later `sd_rescan` calls are idempotent while that device is registered or
mounted; they do not reset the live card with CMD0/ACMD41.
Mount FAT or exFAT media with, for example,
`mount /dev/sd0 /mnt/sdcard`. The mount point is any absolute VFS directory;
`mount` creates it when absent. Refresh an already-open File Manager to add the
mounted drive to its sidebar.
This keeps an uncompleted PCIe load out of the boot path. AHCI is attached at
boot through its kernel adapter, runs ATA IDENTIFY, and publishes usable SATA
disks as `/dev/sataNpN`. NVMe remains initialization-only until its block
adapter is complete; it is not used as an installation target yet.

PCI resource assignment preserves every non-zero firmware BAR without issuing
the destructive all-ones size probe. Fullerene probes and assigns only a BAR
whose address is genuinely zero. This matters for firmware-initialized USB and
card-reader endpoints whose state must survive until their explicit rescan.

Reference: Linux [`drivers/usb/host/pci-quirks.c`](https://github.com/torvalds/linux/blob/master/drivers/usb/host/pci-quirks.c)
and [`drivers/usb/host/xhci-ext-caps.h`](https://github.com/torvalds/linux/blob/master/drivers/usb/host/xhci-ext-caps.h).

## Intel Alder Lake-N xHCI (8086:54ed)

The Chuwi GemiBook XPro (N150) exposes a single xHCI controller at
`00:14.0` with no EHCI companion. Fullerene's USB stack handles this
configuration directly: the Intel USB2/USB3 port-routing quirk is
skipped when no EHCI companion is present, and the xHCI is initialised
through the standard HCRST → configure → start → init-ports sequence.

Extended capabilities (Supported Protocol, Legacy Support) are dumped
during init so USB2/USB3 port classification is visible in the kernel
log. Port protocol parsing assigns the correct reset type (warm reset
for USB3, regular reset for USB2) per port.

### USB hub class driver

The GemiBook XPro has an internal USB 2.0 hub (WCH CH334R,
`1a86:8091`) on a root port. External USB mass-storage devices may be
behind this hub. Fullerene's xHCI stack now enumerates devices behind
external hubs:

1. After Address Device, the device class is probed via a control
   transfer (GET_DESCRIPTOR(device)).
2. If the device is a hub (class `0x09`), SET_CONFIGURATION is issued,
   the Hub Class Descriptor is read for `bNbrPorts`, and a Configure
   Endpoint command updates the slot context with `Hub=1` and
   `NumberOfPorts`.
3. Each downstream port is polled via Get Port Status. Connected ports
   are reset via Set Port Feature (PORT_RESET). After reset, the child
   device is addressed with the correct Route String and Parent Hub
   Slot ID in the slot context.
4. Mass-storage devices behind the hub are enumerated through the
   standard BOT/SCSI path and registered as `/dev/usbN`.

This allows `usb_rescan` to discover Ventoy USB mass-storage devices
whether they are on a root port or behind the internal hub.

### GemiBook XPro USB rescan checklist

The Linux capture for the N150 identifies the machine's xHCI controller as
`00:14.0`, Intel `8086:54ed`, and the I2C-HID touch device as
`AMR13992:00 36B6:C001`. A separate older GemiBook capture contains the
KIOXIA Ventoy device (`30de:6544`, `TransMemory`) on a SuperSpeed xHCI port;
Linux reaches `usb-storage`, SCSI, and an attached removable disk for that
device. These are useful reference identities, but the N150 run itself must
still be diagnosed from its Fullerene Klog.

Before testing, open `Klog Live`, run `usb_rescan`, wait for the scheduler to
finish enumeration, and then run `usb_info`. For a successful root-port
storage path, the important sequence is:

```text
[USB-RESCAN] queue accepted
[USB-RESCAN] poll: controller poll begin
USB: device N descriptor vid=.... pid=....
USB: found BOT mass-storage interface N
USB: device N BOT reset complete ...
USB: xHCI mass-storage device ready ...
USB: registered /dev/usb0 ...
```

Interpret the last line as follows:

- `controllers: xhci poll ports returned` but no device descriptor: port or
  Address Device discovery did not produce a usable candidate.
- `xhci mass-storage enumeration returned` followed by
  `xhci non-hub unsupported device`: configuration parsing did not find a BOT
  or UAS bulk pair; this is not a reason to issue hub requests.
- `xhci hub enumeration begin`: only expected when the descriptor contains a
  real Hub interface (`class 0x09`). Continue with `hub descriptor`, `hub get
  port status`, and downstream-device markers.
- `xhci storage finish begin`: mass-storage enumeration succeeded; inspect
  `bulk out configure`, `bulk in configure`, and `read capacity` next.
- `poll: complete (no device)` followed by `queue retry` through the retry
  count: enumeration
  returned without a disk, so `usb_info` will correctly report no registered
  storage even though the controller is active.

The adjacent state line is especially useful on the N150:

```text
[USB-RESCAN] poll: state enabled=true disks=0 xhci0:ports=11 devices=1 done=0x00000004 [port=3 addr=0 class=00 parent=false]
```

`devices=0` means root-port detection did not leave a candidate. `devices=1`
means the port saw a device and the failure is in Address Device, descriptor,
BOT/UAS, or hub classification. A failed non-hub candidate is retried with
the port state reset; it must not turn the remaining retry attempts into a
misleading stream of `no device` messages.

For the HID touchpad, a physical right-button press should add these Klog Live
edges while the pointer is over the desktop:

```text
[I2C-HID] pointer buttons changed: 0x00->0x02
[I2C-HID] pointer buttons changed: 0x02->0x00
```

The first edge is `MouseDown(Right)` and routes to the desktop or Explorer
context menu; the second is `MouseUp(Right)`. A report ID 6 mouse packet with
button bit `0x02` is used for the GemiBook's physical right button, while
digitizer contact remains the left-button/tap path.

## Google Pixel 4a 5G (Bramble) USB handoff

The AArch64/USB handoff hardware contract, ABL/XBL audit, physical A/B
results, host journal, and recovery state are maintained in
[HARDWARE_aarch64.md](HARDWARE_aarch64.md).

| Item | Current result | Status | Notes |
| --- | --- | --- | --- |
| Target | Pixel 4a 5G / Bramble / `26191JECB00076` | Confirmed | Unlocked; `fastboot boot` only |
| USB2 attach | Fullerene reaches HS attach | Partially successful | The physical pull-up boundary is crossed |
| Enumeration | `device descriptor read/64, error -110` | Not reached | `idVendor=1234` has not appeared |
| Latest A/B | Host-visible `STARTTRANSFER` completion gate was inconclusive | Inconclusive / diagnostic | Run `tmp/fullerene-bramble-loop.910234.0` reached HS attach at `18:24:21`, timed out with descriptor `-110` at `18:24:26`, and recovered as Android SuperSpeed `18d1:4ee7` at `18:24:47`; no `idVendor=1234` appeared |
| Latest A/B | Mock-UTMI 60 MHz source selection was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.922967.0` did not produce a new Fullerene attach and returned to Android SuperSpeed; no `idVendor=1234` appeared |
| Latest A/B | 16-bit PHYIF with `USBTRDTIM=9` still produced `-71` | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.925509.0` reached HS attach at `18:35:52`, produced repeated protocol errors `-71`, and recovered to Android SuperSpeed at `18:36:18`; no `idVendor=1234` appeared |
| Latest A/B | Android controller clock-branch rearm was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.953690.0` reached HS attach at `18:59:20`, timed out with descriptor `-110` at `18:59:25`, and recovered to Android SuperSpeed `18d1:4ee7` at `18:59:47`; no `idVendor=1234` appeared |
| Latest A/B | 500 µs clock-stabilization delay was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.959665.0` reached HS attach at `19:03:57`, timed out with descriptor `-110` at `19:04:02`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:04:23`; no `idVendor=1234` appeared |
| Latest A/B | 20 ms clock-stabilization delay was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.961672.0` reached HS attach at `19:05:26`, timed out with descriptor `-110` at `19:05:31`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:05:52`; no `idVendor=1234` appeared |
| Latest A/B | 20 ms delay without controller-branch rearm was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.967959.0` reached HS attach at `19:10:14`, timed out with descriptor `-110` at `19:10:20`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:10:41`; no `idVendor=1234` appeared |
| Latest A/B | Short first Device Descriptor DATA-IN was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.971596.0` capped the first 64-byte Device Descriptor response at 8 bytes, reached HS attach at `19:12:07`, still timed out with descriptor `-110` at `19:12:12`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:12:32`; no `idVendor=1234` appeared |
| Latest A/B | 60 MHz UTMI with all Android controller branches rearmed was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.986345.0` reached HS attach at `19:22:01`, timed out with descriptor `-110` at `19:22:06`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:22:27`; no `idVendor=1234` appeared |
| Latest A/B | Clearing `GUSB2PHYCFG.U2_FREECLK_EXISTS` was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.989630.0` reached HS attach at `19:24:02`, timed out with descriptor `-110` at `19:24:07`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:24:27`; no `idVendor=1234` appeared |
| Latest A/B | EP0 IN TX FIFO depth correction was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.992481.0` reached HS attach at `19:25:39`, timed out with descriptor `-110` at `19:25:44`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:26:05`; no `idVendor=1234` appeared |
| Latest A/B | Full SuperSpeed handoff was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.996112.0` produced no Fullerene attach; Android SuperSpeed `18d1:4ee7` recovered at `19:28:03`; no `idVendor=1234` appeared |
| Latest A/B | 16-bit PHYIF plus 60 MHz UTMI with controller-branch rearm was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.998824.0` reached HS attach at `19:29:16`, then produced repeated descriptor protocol errors `-71`; Android SuperSpeed `18d1:4ee7` recovered at `19:29:42`; no `idVendor=1234` appeared |
| Latest A/B | XBL-style `NORMAL` TRB for EP0 IN data was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1004534.0` reached HS attach at `19:33:21`, timed out with descriptor `-110` at `19:33:26`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:33:47`; no `idVendor=1234` appeared |
| Latest A/B | Android msm reset-state order was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1007502.0` reached HS attach at `19:35:17`, timed out with descriptor `-110` at `19:35:22`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:35:43`; no `idVendor=1234` appeared |
| Source correction | Direct USB2 handoff now applies Qualcomm DWC3 reference-clock calibration | Implemented / awaiting physical A/B | The direct reuse path was missing the Android msm `update_dwc3_ref_clock()` callback after UTMI-as-PIPE selection; this writes only the source-defined `GUCTL.REFCLKPER` and conditional `GFLADJ` fields |
| Latest A/B | Direct-path DWC3 reference-clock calibration was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1016340.0` reached HS attach at `19:40:45`, timed out with descriptor `-110` at `19:40:51`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:41:11`; no `idVendor=1234` appeared |
| Control | Normal platform initialization did not produce a Fullerene attach | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1018984.0` showed the Fastboot disconnect at `19:42:12` but no Fullerene USB attach or `idVendor=1234`; the direct handoff remains the only attach-reaching path |
| Latest A/B | `ENBLSLPM` enabled on the direct USB2 handoff was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1027688.0` reached HS attach at `19:47:27`, timed out with descriptor `-110` at `19:47:32`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:47:53`; no `idVendor=1234` appeared |
| Latest A/B | Re-publishing the EP0 event ring immediately before Run/Stop was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1034437.0` reached HS attach at `19:52:25`, timed out with descriptor `-110` at `19:52:31`, and recovered to Android SuperSpeed `18d1:4ee7` at `19:52:51`; no `idVendor=1234` appeared |
| Latest A/B | Android msm controller block-reset clock boundary was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1050399.0` applied the `iface/core/sleep/utmi` branch stop, `core_reset` 1 ms assert/deassert, and 10 ms settle before DWC3 rebuild; HS attach at `20:03:25`, descriptor `-110` at `20:03:30`, and Android SuperSpeed `18d1:4ee7` at `20:03:51`; no `idVendor=1234` appeared |
| Latest A/B | Android-sized 4096-byte EP0 event buffer was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1054512.0` used a 4096-byte event buffer with the same direct USB2 handoff; HS attach at `20:06:09`, descriptor `-110` at `20:06:14`, and Android SuperSpeed `18d1:4ee7` at `20:06:35`; no `idVendor=1234` appeared |
| Latest A/B | Skipping the explicit QUSB2 PHY block-reset pulse was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1071095.0` reached HS attach at `20:18:34`, timed out with descriptor `-110` at `20:18:39`, and recovered to Android SuperSpeed `18d1:4ee7` at `20:19:00`; no `idVendor=1234` appeared |
| Latest A/B | Forcing the DWC3 usb31 USB2 retry policy was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1078114.0` reached HS attach at `20:23:13`, timed out with descriptor `-110` at `20:23:18`, and recovered to Android SuperSpeed `18d1:4ee7` at `20:23:39`; no `idVendor=1234` appeared |
| Latest A/B | Add the Android DT HS PHY `ref_clk_src` via the Lito RPMh `xo.lvl` ARC vote (`0x3`) | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1098280.0` reached HS attach at `20:37:15`, timed out with descriptor `-110` at `20:37:21`, and recovered to Android SuperSpeed `18d1:4ee7` at `20:37:42`; no `idVendor=1234` appeared |
| Latest A/B | Current ref-clock vote plus explicit 16-bit PHYIF reproduced `-71` | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1109167.0` reached HS attach at `20:45:00`, produced repeated descriptor/address protocol errors `-71` through `20:45:02`, and recovered to Android SuperSpeed; no `idVendor=1234` appeared. Restore the Bramble 8-bit UTMI baseline for the next control |
| Source audit | Linux `-EPROTO` is not a unique CRC/bitstuff signal | Source audit | Linux documents `-EPROTO` as bitstuff, no response within bus turn-around, or another host-controller error; the 16-bit PHYIF correlation remains a leading framing hypothesis, not a wire-level proof |
| Latest A/B | Android Run/Stop event-ring re-publication on the 8-bit baseline was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1114716.0` reached HS attach at `20:48:07`, returned descriptor `-110` at `20:48:12`, and recovered to Android SuperSpeed at `20:48:33`; no `idVendor=1234` appeared |
| Next A/B | Repeat the Android gadget-start sequence at Run/Stop | Pending / diagnostic | Re-run event-ring publication plus `DEPSTARTCFG`, all 32 transfer resources, both EP0 configs, and initial SETUP arm with `--gadget-restart-at-runstop` |
| Latest A/B | Full Android gadget-start replay at Run/Stop removed the Fullerene attach | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1117988.0` produced no `usb 1-9` HS attach and recovered only to Android SuperSpeed at `20:50:23`; no `idVendor=1234` appeared |
| Next A/B | Gate initial EP0 SETUP arm on Connect Done | Pending / diagnostic | Use `--start-at-connect-done` with the corrected 8-bit baseline and compare the first descriptor boundary |
| Latest A/B | Connect Done-gated initial EP0 SETUP arm was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1122440.0` reached HS attach at `20:52:27`, returned descriptor `-110` at `20:52:32`, and recovered to Android SuperSpeed at `20:52:53`; no `idVendor=1234` appeared |
| Next A/B | Gate initial EP0 SETUP arm on USB Reset | Pending / diagnostic | Use `--start-after-reset` with the corrected 8-bit attach-reaching baseline |
| Latest A/B | USB Reset-gated initial EP0 SETUP arm was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1125421.0` reached HS attach at `20:53:56`, returned descriptor `-110` at `20:54:01`, and recovered to Android SuperSpeed at `20:54:22`; no `idVendor=1234` appeared |
| Next readout | Check whether the host SETUP packet reaches `handle_setup()` | Pending / diagnostic | Use `--signal-early-drop 3`; a pull-up drop means SETUP DMA arrived, while unchanged `-110` places the boundary before software SETUP processing |
| Next A/B | Use XBL-observed DMA object addresses together | Pending / diagnostic | Combine `--xbl-event-dma --xbl-stock-ep0-dma` on the current 8-bit attach-reaching path to test the event ring, SETUP buffer, and initial TRB address space as one DMA differential |
| Latest A/B | XBL-observed event ring, SETUP buffer, and initial TRB addresses together were negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1131854.0` reached HS attach at `20:57:24`, returned descriptor `-110` at `20:57:29`, and recovered to Android SuperSpeed at `20:57:51`; no `idVendor=1234` appeared |
| Next A/B | Flush residual Fastboot EP0 control state before arming | Pending / diagnostic | Use `--ep0-stall-flush` on the 8-bit attach-reaching baseline |
| Next readout | Check USB2 SOF frame liveness before EP0 SETUP delivery | Pending / diagnostic | Use `--signal-early-drop 5`; a drop proves changing DSTS SOF frames, while unchanged `-110` leaves the link-reception boundary unproven |
| Latest A/B | DSTS SOF-frame liveness readout was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1138361.0` reached HS attach at `21:00:19`, returned descriptor `-110` at `21:00:24`, and recovered to Android SuperSpeed at `21:00:45`; no `1234` appeared and no SOF-triggered drop occurred |
| Next A/B | Compare 60 MHz mock-UTMI with SOF liveness | Pending / diagnostic | Use `FULLERENE_AARCH64_USB_UTMI_60MHZ=1 --signal-early-drop 5` while retaining the current ref-clock vote |
| Latest A/B | Corrected SETUP DMA arrival latch saw no host SETUP payload | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1128902.0` reached HS attach at `20:55:50`, returned descriptor `-110` at `20:55:56`, and recovered to Android SuperSpeed `18d1:4ee7` at `20:56:17`; `handle_setup()` saw no non-zero SETUP payload |
| Latest A/B | EP0 stall flush was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1134711.0` reached HS attach at `20:58:51`, returned descriptor `-110` at `20:58:56`, and recovered to Android SuperSpeed `18d1:4ee7` at `20:59:18`; the residual-stall flush did not change the boundary |
| Latest A/B | 60 MHz mock-UTMI with SOF liveness was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1140933.0` reached HS attach at `21:01:57`, returned descriptor `-110` at `21:02:02`, and recovered to Android SuperSpeed `18d1:4ee7` at `21:02:23`; no `1234` appeared and no SOF-triggered drop occurred |
| Next A/B | Initialize the HS PHY before DWC3 CSFTRST | Pending / diagnostic | Use `FULLERENE_AARCH64_USB_HSPHY_BEFORE_RESET=1` on the 8-bit attach-reaching direct handoff to match Android msm's external-PHY-before-core-reset ordering |
| Latest A/B | HS PHY-before-core-reset ordering was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1149688.0` reached HS attach at `21:08:30`, returned descriptor `-110` at `21:08:35`, and recovered to Android SuperSpeed `18d1:4ee7` at `21:08:55`; no `idVendor=1234` appeared |
| Next A/B | Reproduce `-71` with SOF liveness readout | Pending / diagnostic | Use `FULLERENE_AARCH64_USB_PHYIF_16BIT=1 --signal-early-drop 5` with `USBTRDTIM=9`; classify whether the protocol-error branch has changing DSTS SOF frames before EP0 response |
| Latest A/B | 16-bit PHYIF reproduced `-71` with no SOF liveness | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1153733.0` reached HS attach at `21:11:07`, returned repeated descriptor and setup-address errors `-71` through `21:11:10`, and recovered to Android SuperSpeed `18d1:4ee7` at `21:11:33`; no `1234` appeared and no SOF-triggered drop occurred |
| Next A/B | Apply Android's HS PHY termination-tune default | Pending / diagnostic | Bramble's DT lacks `qcom,no-rext-present` and RCAL properties; add the source-equivalent `RTUNE_SEL=1` write at HS PHY offset `0xb4`, bit 0 |
| Latest A/B | Android HS PHY termination-tune default was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1159018.0` reached HS attach at `21:14:29`, returned descriptor `-110` at `21:14:34`, and recovered to Android SuperSpeed `18d1:4ee7` at `21:14:55`; no `idVendor=1234` appeared |
| Next diagnostic | Publish compact DWC3/QSCRATCH/UTMI boundary snapshots | Pending / diagnostic | Read the actual `GUSB2PHYCFG`, QSCRATCH session bits, UTMI source/branch words, `GUSB3PIPECTL`, DSTS, and HS PHY controls at takeover, reset, Run/Stop, and Connect Done before another packet-format A/B |
| Next checks | Capture the mock-UTMI clock source/rate and `GUSB2PHYCFG` at key boundaries | Pending | Android msm requests the UTMI clock at 19.2 MHz, while the Lito GCC source table advertises a 60 MHz mock-UTMI option; add read-only register evidence before another PHY/clock A/B. The detailed table records this source audit |
| Next A/B | Apply Android's HS performance-state core clock to the direct USB2 handoff (`--usb-core-hs-clock`) | Pending / diagnostic | The Bramble DT exposes `qcom,core-clk-rate-hs = 66666667`; the current attach-reaching handoff selects the nominal 133.333333 MHz state. Change only the DWC3 core clock vote/rate and retain the 8-bit UTMI/TRDTIM9 baseline |
| Latest A/B | Android HS performance-state core clock was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1189897.0` used the 66.666667 MHz HS core-clock vote with the unchanged 8-bit UTMI/TRDTIM9 setup; HS attach at `21:35:28`, descriptor `-110` at `21:35:33`, and Android SuperSpeed recovery at `21:35:54`; no `idVendor=1234` appeared |
| Next A/B | Pair the diagnostic 16-bit UTMI interface with Android's 16-bit `USBTRDTIM=5` value | Pending / diagnostic | Repeat the protocol-error branch with `FULLERENE_AARCH64_USB_PHYIF_16BIT=1 FULLERENE_AARCH64_USB_USBTRDTIM=5`; keep the ref-clock vote, core-clock state, DMA, and endpoint layout unchanged to test only the PHYIF/TRDTIM contract |
| Latest A/B | Matched 16-bit UTMI/TRDTIM settings were negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1193516.0` reached HS attach at `21:37:25`, produced repeated descriptor `-71` errors and setup-address failures through `21:37:27`, and recovered to Android SuperSpeed at `21:37:51`; matching `PHYIF=16-bit` with `USBTRDTIM=5` did not produce a valid EP0 response and no `idVendor=1234` appeared |
| Latest A/B | Matched 16-bit UTMI/TRDTIM settings were negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1193516.0` reached HS attach at `21:37:25`, produced repeated descriptor `-71` errors and setup-address failures through `21:37:27`, and recovered to Android SuperSpeed at `21:37:51`; matching `PHYIF=16-bit` with `USBTRDTIM=5` did not produce a valid EP0 response and no `idVendor=1234` appeared |
| Next A/B | Add a 20 ms settle delay after enabling the UTMI clock (`--clock-stable-delay-us 20000`) | Pending / diagnostic | Use the corrected 8-bit UTMI/TRDTIM9 baseline and change only the post-clock-enable wait; this tests whether PLL/RCG settling, rather than packet framing, explains the EP0 boundary |
| Latest A/B | 20 ms UTMI clock-settle delay was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1196977.0` reached HS attach at `21:39:39`, returned descriptor `-110` at `21:39:45`, and recovered to Android SuperSpeed at `21:40:05`; no `idVendor=1234` appeared, so an immediate post-clock PLL/RCG settling delay is not sufficient |
| Next A/B | Apply Android msm's HS Connect Done USB2 policy (`--android-hs-lpm`) | Pending / diagnostic | At HS Connect Done, set `DCFG.LPM_CAP`, clear `DCTL.L1_HIBER_EN`, and restore the DT `HIRD_THRES=0x10` value exactly where Android does; this is a DWC3 link-policy test, not packet wrapping |
| Latest A/B | Android msm HS Connect Done USB2 policy was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1223833.0` reached HS attach at `21:59:11`, returned descriptor `-110` at `21:59:17`, and recovered to Android SuperSpeed `18d1:4ee7` at `21:59:37`; `DCFG.LPM_CAP`/HIRD policy did not move the pre-EP0 boundary and no `idVendor=1234` appeared |
| Next readout | Capture host USB2 control transactions with `/dev/usbmon1` | Pending / diagnostic | Run the unchanged 8-bit baseline while reading the bus-1 usbmon binary stream through the `usbmon` group; correlate control-URB status, length, and setup bytes with the first HS attach and descriptor failure |
| Latest readout | usbmon captured the first 8-bit Device Descriptor failure | Diagnostic evidence | Run `tmp/fullerene-bramble-loop.1229271.0` attached HS at `22:02:31`; usbmon recorded `GET_DESCRIPTOR(Device)` (`80 06 01 00 00 00 40 00`) submitted at `22:02:36.579408` and completed at `22:02:36.811699` with `status=-71` and zero data. The dmesg summary later showed `-110`; this establishes a host-controller protocol-error completion before the final timeout, but not the wire-level CRC/bitstuff cause |
| Next readout | Capture usbmon on the reproducible 16-bit `-71` branch | Pending / diagnostic | Repeat the same bus-1 capture with `FULLERENE_AARCH64_USB_PHYIF_16BIT=1 FULLERENE_AARCH64_USB_USBTRDTIM=5`; compare descriptor completion status, data length, and retry cadence against the 8-bit capture |
| Latest readout | usbmon confirmed the immediate 16-bit protocol-error branch | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1234879.0` attached HS at `22:06:35`; each `GET_DESCRIPTOR(Device)` completion returned `status=-71` with zero data, with three failures from `22:06:35.758404` to `.758784` and repeated groups thereafter. The 16-bit path is an earlier/faster host-controller failure than the 8-bit path; no `idVendor=1234` appeared |
| Source correction | Restore Android msm's `dwc3_set_mode(DEVICE)` controller-mode tail | Source correction applied | The direct handoff selected `PRTCAPDIR=DEVICE` but omitted the second GCTL write: `U2RSTECN=1`, `SOFITPSYNC=0`, `PWRDNSCALE=2`, and `U2EXIT_LFPS=1`. This is a controller-mode A/B against the usbmon-confirmed pre-EP0 protocol-error boundary, not packet wrapping |
| Latest A/B | Android msm `dwc3_set_mode(DEVICE)` controller-mode tail was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1248371.0` reached HS attach at `22:16:47`; usbmon still completed `GET_DESCRIPTOR(Device)` with zero data and `-71` at `22:16:52.729609`–`.729833`; Android SuperSpeed `18d1:4ee7` recovered at `22:17:13`; no `idVendor=1234` appeared |
| Next diagnostic | Compare the complete Android msm HS PHY init against the direct handoff | Pending / source-guided | Re-check the femto PHY's write order and field semantics around `CFG0`, `UTMI_CTRL5`, `COMMON0/1/2`, `CTRL1/2`, `RTUNE_SEL`, and `REFCLK_CTRL`; select only a still-unmatched source-defined field for the next A/B |
| Next readout | Time-encode the live DWC3 USB2 link state on the 16-bit `-71` branch (`--signal-cmd-gate lnkraw --observe-secs 16`) | Pending / diagnostic | Stop immediately after the normal ~15 s Fullerene attach delay, while the host is still retrying the first descriptor; use the existing DCTL stop/disconnect readout to distinguish a live link-FSM state from a pre-transaction wedge |
| Latest readout | 16-bit `-71` branch with one-second `lnkraw` observation was inconclusive | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1167343.0` reached HS attach at `21:20:27`, produced repeated descriptor/address `-71` errors through `21:20:29`, and recovered to Android SuperSpeed at `21:20:53`; the one-second observation ended before the normal ~15 s attach delay, no host-visible DCTL disconnect was observed, and no `idVendor=1234` appeared |
| Latest readout | 16-bit `-71` branch with twenty-second `lnkraw` observation was inconclusive | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1171830.0` reached HS attach at `21:23:17`, produced repeated descriptor/address `-71` errors through `21:23:19`, and recovered to Android SuperSpeed at `21:23:42`; the gate stop occurred after the host had already abandoned enumeration, so no exact DSTS link state is claimed and no `idVendor=1234` appeared |
| Latest readout | 16-bit `-71` branch with sixteen-second `lnkraw` observation was inconclusive | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1176736.0` reached HS attach at `21:26:10`, produced repeated descriptor/address `-71` errors through `21:26:12`, and recovered to Android SuperSpeed at `21:26:36`; no host-visible DCTL disconnect was observed, the timing channel did not yield an exact DSTS state, and no `idVendor=1234` appeared |
| Next readout | Inspect the local UP1A.231105.001.B2 Factory bootloader package for ABL/XBL USB register or error-path evidence | Pending / read-only | The package matches the connected Bramble's bootloader/radio identifiers. This is static binary inspection only; no flashing or boot action is part of this step |
| 2026-09-01 | Factory ABL HS-PHY source audit | Source evidence | The bootloader's `usb_shared_hs_phy_init` clears `UTMI_CTRL5.ATE_RESET`, `TEST1[6,4]`, `COMMON0.VATESTENB`, and `TEST0[7:0]`, then waits 20 us after the suspend-N handoff and another 20 us after clearing `CFG0.CMN_CTRL_OVERRIDE_EN`; the current Linux-derived path did not perform this cleanup |
| Next A/B | Apply Factory ABL's additional QUSB2 HS-PHY cleanup (`--abl-shared-hs-phy`) | Pending hardware run | Keep the attach-reaching 8-bit handoff unchanged and add only the ABL-observed test/ATE cleanup plus two 20 us settle delays; usbmon will classify the first `GET_DESCRIPTOR(Device)` completion |
| Latest A/B | Factory ABL HS-PHY cleanup and settle ordering was negative | Negative / diagnostic | Run `tmp/fullerene-bramble-loop.1280378.0` reached HS attach at `22:41:11`; usbmon still showed the Device Descriptor URB with zero data and `-71` completions at `22:41:16.746493` and `22:41:17.094606`–`.094846`; Android SuperSpeed `18d1:4ee7` recovered at `22:41:37`; no `idVendor=1234` appeared |
| 2026-09-01 | Factory ABL/XBL Protocol Error path audit | Source audit | ABL's `Protocol Error` and `CRC Error` strings are entries in the generic UEFI status-name table used by the common status formatter, not evidence of a USB wire-error branch. The USB-specific path instead contains a bounded DWC3 `DEPCMD` busy wait at `0x0a60c80c`, `ep_cmd_write failed, cmd_type`, EP0 `endxfer`, and `recover ctrl state machine`; the RUMI-only `UTMI MMCM` messages are a separate debug branch. The ABL HS-PHY cleanup A/B was already negative, so payload wrapping is not the next control |
| Next readout | Capture the Fullerene pre-EP0 DWC3 command/state boundary against the ABL endpoint-command path | Pending / diagnostic | Keep the proven 8-bit attach path and use the retained trace/usbmon evidence to distinguish `DEPCMD`/EP0 ownership failure from a lower USB2 PHY framing failure; do not treat host `-71` alone as proof of CRC or bitstuff corruption |
| 1301400.0 | Use Factory ABL's observed narrow DWC3 device-event mask (`--abl-devten`, `DEVTEN=0x47`) on the attach-reaching 8-bit handoff | HS attach at `22:56:26`; `GET_DESCRIPTOR(Device)` was submitted to unaddressed `dev=0` at `22:56:32.418396` and completed with zero data and `status=-71` at `.418531`, `.418624`, and `.418745`; Android SuperSpeed `18d1:4ee7` recovered at `22:56:53`; no `1234` attach | Negative / matching ABL's `DEVTEN` mask did not move the first EP0 protocol-error boundary. The usbmon binary stream shows immediate xHCI completion errors, but no wire-level CRC/bitstuff diagnosis; packet wrapping is not implicated by this A/B | Artifact SHA `45a1dd99ec63a43183691715e75cb4608579ada30a3b1a962a078d0fb947c62`; usbmon `tmp/fullerene-bramble-loop.1301400.0/usbmon-bus1.bin` SHA `913d0f1a3a3a8f11505aaf616e53d8f3250bce45ecaa17c1f9ffcb4b2c0e9614`; QEMU preflight and `fastboot boot` were accepted; no flash/erase operation was attempted |
| 1315217.0 / 1319391.0 / 1322401.0 | Use existing `mrad`, `pub`, and `dstat` diagnostic gates around the first EP0 transaction | `mrad`: first descriptor completion was `-2` after about 5.48 s, then retries were immediate `-71`; `pub` did not yield an exact progress code; `dstat` produced no host-visible extra DCTL reattach cycle. All runs reached HS attach and recovered to Android SuperSpeed; no `1234` attach | Negative / inconclusive readout. The gates did not expose a trustworthy internal progress value, and the `-71` retry cluster still follows the same first-descriptor boundary. Keep these as timing/readout controls, not as evidence that packets need wrapping | Raw usbmon SHA: `8871bcd9d6ab8559d9c5b5571616205f419518932b8fdf955dbd64a8afd7b668` (`mrad`), `40526e1e2b3e0f4392ba52b53973409730ed9e95b2b7159d0a466e5db1fbdaa3` (`pub`), `fb40c365c0c8be7cf87e8f324357dff2870a8d83b273321902a246979224a287` (`dstat`); each run returned by watchdog; no flash/erase operation was attempted |
| Next control | Instrument the Fullerene EP0 `DEPCMD`/event ownership boundary before the first descriptor | Pending diagnostic | Capture the local DWC3 device-event status, EP0 command busy/complete state, and event-ring consumer state immediately before and after the first `GET_DESCRIPTOR`; compare with ABL's endpoint-command busy poll. Keep the 8-bit PHY and response TRB unchanged |
| Next readout | Stop at the first observed EP0 SETUP (`--signal-cmd-gate setup-cut`) | Pending diagnostic | This is a timing-sensitive protocol boundary test: a host-visible disconnect means SETUP DMA/processing reached Fullerene, while the unchanged `-110`/`-71` sequence means the failure is earlier in USB2 RX, DWC3 event delivery, or EP0 ownership. It does not identify CRC versus bitstuff error by itself |
| 1355633.0 | Stop at the first observed EP0 SETUP (`--signal-cmd-gate setup-cut`) | HS attach at `23:36:55`; no diagnostic disconnect; descriptor read timed out with `-110` at `23:37:00`, and Android SuperSpeed `18d1:4ee7` recovered at `23:37:20`; no `1234` attach | Negative boundary readout. The SETUP latch was not reached before the host's descriptor timeout under the direct DCTL stop test, so payload wrapping cannot be the current lever. usbmon shows the descriptor submit at `23:36:55.078377`, `-2` at `23:37:00.106461`, then `-71` retries at `.717188` and `.717306`, all with zero data | Artifact SHA `0b97288476e15818b3c33374912ffe5383b650fa8ae0b6c5573c0be7702796c9`; usbmon `tmp/fullerene-bramble-loop.1355633.0/usbmon-bus1.bin` SHA `7daf8f1e0fb032798646875a8be36d9feac6d1d625f0cef2fcaa504780e9acbd`; `fastboot boot` only, no flash/erase |
| Next readout | Stop at the first consumed DWC3 event (`--signal-cmd-gate event-cut`) | Pending diagnostic | This precedes SETUP parsing: a diagnostic disconnect would prove event-ring delivery and move the suspect to EP0 ownership/command handling; unchanged `-110` would move it earlier to DWC3 event delivery or USB2 link reception |
| 1362989.0 | Stop at the first consumed DWC3 event (`--signal-cmd-gate event-cut`) | HS attach at `23:42:19`; no diagnostic disconnect; descriptor read timed out with `-110` at `23:42:24`; Android SuperSpeed `18d1:4ee7` recovered at `23:42:45`; no `1234` attach | Negative boundary readout. The event-delivery latch was not reached before the host timeout, so the response TRB and packet wrapping remain downstream of the observed failure. usbmon recorded the descriptor submit at `23:42:19.657436`, `-2` at `23:42:24.715453`, then zero-length `-71` retries at `.295467`, `.295586`, and `.295817` in the next retry burst | Artifact SHA `d9736efba957825cf6eec402b3e588a1bb82df0c28bff528c81ac12658d69cb0`; usbmon `tmp/fullerene-bramble-loop.1362989.0/usbmon-bus1.bin` SHA `c5548dbd800f0074b052972b814650a8a4fca0b92534f98fc20ccd215ad7e45d`; `fastboot boot` only, no flash/erase |
| Next control | Audit/fix DWC3 event-ring producer/consumer ownership before EP0 SETUP | Pending source-guided A/B | Compare event-ring base/size/count publication and consumer acknowledgement against Android/ABL; retain the 8-bit PHY and descriptor response unchanged, because no software SETUP boundary was reached |
| Next readout | Gate on the retained EP0 SETUP `STARTTRANSFER` state (`--signal-cmd-gate ep0armed`) | Pending hardware run | Keep the exact attach-reaching 8-bit path and make the existing `EP0_SETUP_ARMED` state host-visible through the proven DCTL stop/park readout. A positive result means the command/resource arm exists before the missing event; an unchanged timeout means the arm state is also absent or not retained. This is a state readout only: no PHY, TRB, or response-data change |
| Next A/B | Apply Factory ABL/Qualcomm msm's EP0 `SETEPCONFIG` fields (`--abl-ep-config`) | Pending hardware run | ABL emits `P0=(3<<22)|(maxpacket<<3)|(type<<1)` plus the IN FIFO number, and `P1=0x300|(endpoint-address<<25)`; for EP0 OUT/IN at 64 bytes this is `P0=0x00c00200`, `P1=0x00000300/0x02000300`. Retain the proven 8-bit PHY, event ring, TRB, and response bytes |
| 1446531.0 | Apply the earlier reconstructed ABL EP0 `SETEPCONFIG` pair (`--abl-ep-config`) | HS attach at `00:42:50`; the first Device Descriptor submit at `00:42:50.905414` timed out with `-2` at `00:42:56.330523`, then three zero-length retry completions were `-71` at `00:42:56.554591`, `.554723`, and `.554884`; Android SuperSpeed `18d1:4ee7` recovered at `00:43:16`; no `1234` attach | Negative / this run used the earlier reconstructed `P0/P1/P2` values, not the later disassembly-confirmed ABL/Qualcomm msm fields. It is not evidence against the exact `burst-size/FIFO/endpoint-address/0x300` configuration now queued; the host still received no descriptor data, so packet wrapping remains downstream | Artifact SHA `36a57e4c0292c803e5cde9f4d4180b1ece8cfaed9374f7c4034b563832258bc7`; usbmon `tmp/fullerene-bramble-loop.1446531.0/usbmon-bus1.bin` SHA `1e2d27ee2f3565d6425fad343433f2288ef9888c7d086d9153e18fb743283281`; `fastboot boot` only, no flash/erase |
| 1454145.0 | Re-publish the EP0 event ring immediately before Run/Stop (`--event-ring-at-runstop`) | HS attach at `00:47:33`; the first Device Descriptor submit at `00:47:33.797487` timed out with `-2` at `00:47:38.954438`, then three zero-length retry completions were `-71` at `00:47:39.432823`, `.432958`, and `.433061`; Android SuperSpeed `18d1:4ee7` recovered at `00:48:01`; no `1234` attach | Negative / moving event-ring publication to the Run/Stop boundary did not expose an EP0 response or change the pre-response Protocol Error shape. No packet payload was captured, so wrapping is still downstream | Artifact SHA `d213ff8aac87b478a0d6df27d09d3a931c8c17f02793932d2c0d85772a021318`; usbmon `tmp/fullerene-bramble-loop.1454145.0/usbmon-bus1.bin` SHA `1242d042232def2ce5d41ded4912d2cc965af6c9ce0ebaf30a3667882b0e3d8a`; `fastboot boot` only, no flash/erase |
| 1457800.0 | Time-encode EP0 arm status at the first descriptor boundary (`--signal-cmd-gate armstat`) | HS attach at `00:49:32`; the first Device Descriptor submit at `00:49:32.335396` timed out with `-2` at `00:49:37.738470`, then three zero-length retry completions were `-71` at `00:49:37.974211`, `.974307`, and `.974427`; Android SuperSpeed `18d1:4ee7` recovered at `00:49:58`; no `1234` attach or diagnostic disconnect | Negative / the EP0 arm-status gate produced no trustworthy host-visible state code before timeout. Combined with the unchanged zero-length `-71` cluster, this leaves EP0 command/event ownership or lower USB2 controller state unresolved; it provides no evidence that packet wrapping can repair the path | Artifact SHA `186c637a0409884b21ac16f4d19cc39eb3f4bfad599e53e5046c7d863bd49478`; usbmon `tmp/fullerene-bramble-loop.1457800.0/usbmon-bus1.bin` SHA `177659bdfc510b2716639003301e3ac117477e249c9919c0a7f6dba945afb425`; `fastboot boot` only, no flash/erase |
| 1471203.0 | Re-test EP0 arm status with a host-visible timing stop (`--signal-cmd-gate armdelay --observe-secs 1`) | HS attach at `00:59:00`; the first Device Descriptor submit at `00:59:00.448497` timed out with `-2` at `00:59:05.546477`, then three zero-length retry completions were `-71` at `00:59:06.084010`, `.084134`, and `.084258`; Android SuperSpeed `18d1:4ee7` recovered at `00:59:27`; no `1234` attach and no diagnostic disconnect | Negative / after fixing direct-path status recording, the timing stop still produced no host-visible boundary and did not change the zero-length Protocol Error cluster. The result is inconclusive about arm status, but it confirms that neither APSS timing nor DCTL stop exposes a usable readout on this handoff; packet wrapping remains downstream | Artifact SHA `dad67e8282007848f198427ece9239e747d851702ed1b63bdd4e1422cb04d331`; usbmon `tmp/fullerene-bramble-loop.1471203.0/usbmon-bus1.bin` SHA `b6d2a8024cca92a77dbdbcd5c462dff8bff1af1cf7067669714b66be4e68b982`; `fastboot boot` only, no flash/erase |
| `2026-09-02` | Separate the source meaning of host `Protocol Error` (`-71`) from a wire-payload failure | Source evidence | Linux/xHCI can report `-EPROTO` for a transaction error without proving CRC/bitstuff corruption. In the captured `GET_DESCRIPTOR(Device)` URBs, the completion has `len=0`/`cap=0`: no descriptor bytes reached the host. Android msm's initial EP0 `STARTTRANSFER` is plain `STARTTRANSFER`; ABL's `0x406` is `CMDACT(0x400) + STARTTRANSFER(0x6)`, not `CMDIOC`, and its call site is in the event/request path. There is no command-format delta to fix with wrapping or an IOC bit; next control is the ABL/Fullerene EP0 event-to-request ownership boundary |
| `Source correction` | Keep initial EP0 `CONTROL_SETUP` eager; do not infer its arm point from ABL `0x406` | Source evidence corrected | Android msm/Linux prepare the initial `CONTROL_SETUP` TRB and immediately issue plain `STARTTRANSFER`. Linux explicitly avoids waiting for `XferNotReady(DATA)` because back-to-back SETUP packets can be missed. The deferred A/B below is therefore an invalid control for the initial-SETUP question; ABL `0x406` must be located in a later request/phase call site |
| 1501851.0 | Historical deferred EP0 arm experiment (`--xbl-deferred-setup`) | HS attach at `01:20:56`; the first Device Descriptor read timed out with `-110` at `01:21:02`; Android SuperSpeed `18d1:4ee7` recovered at `01:21:23`; no `1234` attach | Negative / invalid control. This run deliberately deferred the initial SETUP based on a misreading of ABL `0x406`; it does not test the source-confirmed eager SETUP path and provides no evidence about PHY or packet wrapping. The boot artifact SHA is `a1112486abfcafb93b5cdb57b3a8534c335d4b8fb271296bfaeaeba765030bf7`; no flash/erase operation was attempted |
| 1506270.0 | Historical deferred EP0 arm plus event-cut (`--xbl-deferred-setup --signal-cmd-gate event-cut`) | HS attach at `01:23:26`; the first Device Descriptor read timed out with `-110` at `01:23:32`; Android SuperSpeed `18d1:4ee7` recovered at `01:23:53`; no `1234` attach and no extra diagnostic disconnect | Negative / invalid control. The event-cut latch did not fire, but because the initial SETUP was intentionally deferred this cannot establish that the normal eager event path is absent. Boot artifact SHA `5a1534fd6ff8566f1e211ad9e568196f41e3db538b231a5059e987aa14b7d260`; no flash/erase operation was attempted |
| 1512823.0 | Capture the historical deferred/event-cut control under usbmon | HS attach at `01:27:05`; usbmon submitted `GET_DESCRIPTOR(Device)` at `01:27:05.557407`, completed `-2` at `01:27:11.050446`, then returned zero-length `-71` retries at `01:27:11.266565`, `.266737`, and `.266899`; Android SuperSpeed `18d1:4ee7` recovered at `01:27:31`; no `1234` attach | Negative / invalid control plus diagnostic capture. Every failing completion had `len=0`/`cap=0`, so no descriptor payload reached the host; wrapping cannot repair this boundary. There was no extra diagnostic disconnect. Boot artifact SHA `5a1534fd6ff8566f1e211ad9e568196f41e3db538b231a5059e987aa14b7d260`; usbmon SHA `a3564e4cf63694adefa9b8c548906c8fe98ae4a737e2f642ce78cbcc7550ee13`; `fastboot boot` only, no flash/erase |
| `Next control` | Reverse-engineer every ABL caller of `0x406` and map its event/status, endpoint direction, and control phase | Pending source-guided | Keep the canonical eager `CONTROL_SETUP` arm unchanged. The next A/B must reproduce only the call-site behavior proven by the disassembly, then correlate DWC3 event-ring state with usbmon's first zero-data completion |
| 1389970.0 | Apply the earlier, misread ABL EP0 parameter pair (`--abl-ep-config`) | HS attach at `00:02:06`; descriptor submit at `00:02:06.639493`, first completion `-2` at `00:02:12.042439`, then zero-length `-71` retries at `.275920`, `.276054`, and `.276148`; Android SuperSpeed `18d1:4ee7` recovered at `00:02:32`; no `1234` attach | Negative / this run used `P1` bit 12 plus `PAR2=1`, which is not the ABL command layout. It is therefore not evidence against the exact ABL pair; usbmon contains no descriptor data | Artifact SHA `0360a54fd1a1c1cefd309eed05146f3827c5458b80e3d0e612004dc506159442`; usbmon `tmp/fullerene-bramble-loop.1389970.0/usbmon-bus1.bin` SHA `8e91f2acd492fe5cd7f2d49afe3cb8ba5b5c9b65eef3d2b5dd6ace132c586f32`; `fastboot boot` only, no flash/erase |
| 1376564.0 | Gate on retained EP0 SETUP `STARTTRANSFER` state (`--signal-cmd-gate ep0armed`) | HS attach at `23:52:22`; descriptor read timed out with `-110` at `23:52:27`; Android SuperSpeed `18d1:4ee7` recovered at `23:52:48`; no `1234` attach or diagnostic disconnect | Negative state readout. The existing arm-state gate did not produce a host-visible stop before timeout, so this run does not prove that EP0 was armed; it also did not move the pre-response boundary | Artifact SHA `5f7799a2ff74c09b36257b818957e492d2ddc52f02bd6557fa1bf0bb95943070`; usbmon `tmp/fullerene-bramble-loop.1376564.0/usbmon-bus1.bin` SHA `341f268203d4d053df94c2319b998871cced0c5b50e337f4bee20dc229d912`; watchdog return, `fastboot boot` only |
| Next control | Map ABL's `0x406` callers to the actual EP0 request/phase boundary | Pending source-guided | Keep initial `CONTROL_SETUP` eager in Fullerene. Distinguish the event ring's notification from the software request queue and reproduce only the proven endpoint direction/status transition |
| 2026-09-02 | Factory ABL `0x406` caller audit | Source evidence corrected | `0x406` is `CMDACT | STARTTRANSFER`. ABL reaches it from the request-building path at `0x29e4c -> 0x2a4a0` and from the endpoint start helper at `0x2a46c -> 0x2a4a0`; both receive a logical endpoint and direction, and the helper stores the returned resource index. The `DwcConfigureEP` tail still contains no initial EP0 `STARTTRANSFER`, but that absence does not prove deferred initial SETUP: the call sites identify `0x406` as a later request/phase starter, so the initial arm boundary remains to be located |
| Next readout | Gate on a raw DWC3 device-error event (`--signal-cmd-gate deverr`) | Pending hardware run | Enable the normal broad `DEVTEN` mask and stop at the first consumed `ERRATIC_ERROR`/`CMD_COMPLETE`/`OVERFLOW` device event. This isolates a DWC3-internal error from the host's ambiguous `-71`; keep PHY, TRB, response data, and the ABL EP0 parameter test unchanged |
| 1415971.0 | Gate on a raw DWC3 device-error event (`--signal-cmd-gate deverr`) | HS attach at `00:21:33`; no diagnostic disconnect; the first Device Descriptor submit at `00:21:33.540488` timed out with `-2` at `00:21:38.890441`, then retry completions were `-71` at `00:21:39.175245`, `.175357`, and `.175503`; Android SuperSpeed `18d1:4ee7` recovered at `00:21:59`; no `1234` attach | Negative device-event readout. No consumed `ERRATIC_ERROR`/`CMD_COMPLETE`/`OVERFLOW` event reached the gate before the host's `-110` boundary, so the host `-71` is not accompanied by a captured DWC3 device-error event. usbmon still has zero response data; wrapping is downstream of this failure | Artifact SHA `62e8e64c8f7c66b5fd784b9740923310adcf8ef7f7d2368cd0f6080837f92d12`; usbmon `tmp/fullerene-bramble-loop.1415971.0/usbmon-bus1.bin` SHA `4d890262b07dbcb3032b7cc03236d765559991ba4c7cb0f053a2e4101b920594`; `fastboot boot` only, no flash/erase |
| 1429169.0 | Time-encode the baseline DWC3 `DSTS.USBLNKST` at the live first-descriptor boundary (`--signal-cmd-gate lnkraw`) | HS attach at `00:30:28`; no diagnostic disconnect; the first Device Descriptor submit at `00:30:28.154362` timed out with `-2` at `00:30:33.418438`, then three zero-length retry completions were `-71` at `00:30:33.790998`, `.791128`, and `.791247`; Android SuperSpeed `18d1:4ee7` recovered at `00:30:54` and retried at `00:30:55`; no `1234` attach | Negative / inconclusive link-state readout. `lnkraw` produced no trustworthy host-visible DCTL stop, so no raw `DSTS.USBLNKST` value is claimed. It nevertheless confirms the same pre-response boundary: `-71` carries no data, and no DWC3 device-error gate fired. This still does not identify CRC/bitstuff versus another controller condition; packet wrapping remains downstream | Artifact SHA `a88903511ae50c706474f23520bb82ab7712c8b72b670d1a9eb887f366007dae`; usbmon `tmp/fullerene-bramble-loop.1429169.0/usbmon-bus1.bin` SHA `b8981ee7ad4eadd95213d056d5b80efe6bfd4a2a4326f4b505b95134b5f19a2a`; `fastboot boot` only, no flash/erase |
| Source correction | Re-read Factory ABL `DwcConfigureEP` parameter packing | Source evidence corrected | The initial `SETEPCONFIG` is `P0=(3<<22)|(maxpacket<<3)|(type<<1)` plus the IN FIFO number, and `P1=0x300|(endpoint-address<<25)`, with `P2=0`; the following `SETTRANSFRESOURCE` stores `P0=1`. The earlier `1389970.0` and `1446531.0` reconstructions did not test this exact sequence. |
| Next readout | Time-encode the baseline DWC3 `DSTS.USBLNKST` at the live first-descriptor boundary (`--signal-cmd-gate lnkraw`) | Pending hardware run | Keep the 8-bit attach-reaching path and no PHY/TRB/EP-config changes; publish the raw link-state nibble through the existing DCTL stop timing to distinguish a DWC3 link-FSM wedge from an EP0 command/event-ownership failure |

| `1537260.0` | Re-run the source-confirmed eager EP0 path without the attach-reaching `--start-after-connect` control | Fastboot device `52` disconnected at `01:42:52`; no Fullerene HS attach or Fullerene descriptor URB appeared; Android SuperSpeed `18d1:4ee7` returned as devices `53`/`54` at `01:43:32`/`01:43:34`; no `1234` attach | Negative / no Protocol Error classification: the host never submitted a Fullerene `GET_DESCRIPTOR(Device)`, so this run stopped before the `-71` boundary. Because `--start-after-connect` was omitted, this is not the attach-reaching control used by the earlier `-71` runs; it is not evidence for packet wrapping or against the USB2 PHY. The usbmon bus was correctly `/dev/usbmon2`; `fastboot boot` only, no flash/erase | Artifact SHA `92f7bf76d1a3c4dc5e45c6bd4491deab7c3b105fbfa48d80b39ccdd0e2e85734`; usbmon SHA `6e2fe2d6e1929ace1ac189aa4afc5de0d91fc3ff092097463c033d39adb8530b` |
| `1549596.0` | Re-run the source-confirmed eager EP0 attach-reaching profile, but capture the wrong USB bus (`/dev/usbmon2`) | Fullerene HS attach on USB bus 1 at `01:51:34`; host `device descriptor read/64` timed out with `-110` at `01:51:39`; Android SuperSpeed `18d1:4ee7` returned at `01:52:00`; no `1234` attach | Inconclusive / the usbmon stream was bus 2, which contains Fastboot/Android SuperSpeed traffic; the Fullerene USB2 control URB is on bus 1, so this capture cannot classify `-71` or payload length. The next run must use `/dev/usbmon1`; `fastboot boot` only, no flash/erase | Artifact SHA `3d739e3533d525c115209fefc29c96f2a6d7f41a311f7e809f3cd7f1dafabacc`; wrong-bus usbmon SHA `f4a71ffc4848724a54102cbe73c6144540d6343925e0a6ac26ee1a62461b819e` |
| `1553076.0` | Re-run the source-confirmed eager EP0 attach-reaching profile with the correct USB2 capture (`/dev/usbmon1`) | Fullerene HS attach on bus 1 at `01:53:25`; `GET_DESCRIPTOR(Device)` submitted at `01:53:25.725490`, usbmon completion `-2` at `01:53:31.082402`, followed by zero-length `-71` completions at `01:53:31.362002`, `.362132`, and `.362226`; Android SuperSpeed `18d1:4ee7` recovered at `01:53:52`; no `1234` attach | Negative / this is the reproducible host Protocol Error branch. Every failing completion had `len=0`/`cap=0`, so no descriptor payload reached xHCI; this is upstream of any software payload wrapping. The host's dmesg summarized the first read as `-110`, while usbmon exposed the preceding `-2` and immediate `-71` retry cluster; `fastboot boot` only, no flash/erase | Artifact SHA `3d739e3533d525c115209fefc29c96f2a6d7f41a311f7e809f3cd7f1dafabacc`; usbmon SHA `da2a17f37baa5ff46a8a5473f1a47ee152a4db7d87765ecefeede58ecf8856de` |
| `Next readout` | Stop at the first observed EP0 SETUP on the canonical attach-reaching eager path (`--signal-cmd-gate setup-cut`) | Pending hardware run | Capture `/dev/usbmon1` in parallel. A host-visible stop proves that DWC3 DMAed/consumed SETUP and moves the suspect to EP0 response/IN ownership; unchanged zero-data `-2`/`-71` leaves the failure in USB2 RX, event delivery, or SETUP ownership. This is a readout only: no PHY, TRB, or response-payload change |
| `1566338.0` | Stop at the first observed EP0 SETUP on the canonical attach-reaching eager path (`--signal-cmd-gate setup-cut`) | Fullerene HS attach on bus 1 at `02:02:20`; no diagnostic disconnect proving `handle_setup()` reached; `GET_DESCRIPTOR(Device)` submitted at `02:02:20.458407`, completed `-2` at `02:02:25.610477`, then zero-length `-71` retries at `02:02:26.097669`, `.097769`, and `.097887`; Android SuperSpeed `18d1:4ee7` recovered at `02:02:46`; no `1234` attach | Negative boundary readout. The SETUP latch was not reached before the host abandoned the first descriptor, while usbmon again showed no returned bytes. The Protocol Error is therefore upstream of descriptor payload formatting, at USB2 RX/DWC3 event delivery/EP0 SETUP ownership; `-71` alone still does not distinguish CRC from controller state. `fastboot boot` only, no flash/erase | Artifact SHA `05e335d1f9ef0c6ab7bdf0e34024b3efd0fdb2fe2e4d3f477333b6b0142d4b5c`; usbmon SHA `91e4b1d267cf0ce8af109172f7b050f7ba045295b8ce43e5ea31917796b9c675` |
| `Next A/B` | Match Android msm's 4096-byte control event buffer (`--event-ring-size-4096`) while retaining `setup-cut` | Pending hardware run | ABL uses `GEVNTSIZ0=0xf0`, while Android msm allocates/programs `DWC3_EVENT_BUFFERS_SIZE=4096`. Keep the canonical attach-reaching eager EP0 path, bus-1 usbmon, and all PHY/TRB/response settings unchanged; a SETUP-cut stop would prove whether the smaller ABL-sized ring is blocking event delivery |
| `1571023.0` | Match Android msm's 4096-byte control event buffer (`--event-ring-size-4096`) while retaining `setup-cut` | Fullerene HS attach on bus 1 at `02:05:01`; no diagnostic disconnect proving `handle_setup()` reached; `GET_DESCRIPTOR(Device)` submitted at `02:05:01.062409`, then zero-length `-71` completion cluster at `02:05:06.701810`, `.704497`, and `.704614`; Android SuperSpeed `18d1:4ee7` recovered at `02:05:27`; no `1234` attach | Negative / changing `GEVNTSIZ0` from the ABL-sized `0xf0` to Android's 4096 did not move the first Protocol Error boundary. The host still received no descriptor payload, so wrapping remains downstream; this A/B points away from event-buffer length alone and leaves USB2 RX, DWC3 event delivery, or EP0 SETUP ownership unresolved. `fastboot boot` only, no flash/erase | Artifact SHA `23cbafd8fcd20c308379cc66d0fca410792a67a2a70d44fbc45bf25b57718870`; usbmon SHA `ba1d7404a0e21605bcf2a4623b91eabf9b865b4226d09d1e0bdfa0502ea246c1` |
| `1578831.0` | Repeat the canonical attach-reaching `setup-cut` with a valid host-attach window (`--signal-cmd-gate setup-cut --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:10:31`; no diagnostic disconnect proving `handle_setup()` reached; `GET_DESCRIPTOR(Device)` submitted at `02:10:31.494445`, completed `-2` at `02:10:36.618471`, then zero-length `-71` retries at `02:10:37.132324`, `.132511`, and `.132637`; Android SuperSpeed `18d1:4ee7` recovered at `02:10:57`; no `1234` attach | Negative / this is a valid timing-window readout, unlike the earlier one-second gates. The SETUP latch still did not fire before the host's `-2`/`-71` boundary, and usbmon captured no descriptor bytes. Under the current latch, the failure is before SETUP DMA/`handle_setup()`—USB2 RX, DWC3 endpoint state, or DMA/event ownership remain candidates; `-71` alone still does not identify CRC/bitstuff. Packet wrapping is downstream | Artifact SHA `54c472e77cb85bd8703cf7bfe65e4cf5c8c846dff462d144bb030961f3c2b8c1`; usbmon SHA `9548c67192d364fb7e73e27c2197ce725cac33f8a9b4fb5a8464a2479c744562`; `fastboot boot` only, no flash/erase |
| `1587153.0` | Gate on the first consumed DWC3 event with a valid host-attach window (`--signal-cmd-gate event-cut --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:16:11`; no diagnostic disconnect proving event-ring consumption; `GET_DESCRIPTOR(Device)` submitted at `02:16:11.459500`, completed `-2` at `02:16:16.586532`, then zero-length `-71` retries at `02:16:17.098996`, `.099101`, and `.099198`; Android SuperSpeed `18d1:4ee7` recovered at `02:16:37`; no `1234` attach | Negative / the event-consumer latch also remained clear in the valid window. Together with `1578831.0`, no DWC3 event or SETUP payload was observed before the host's Protocol Error cluster; the current boundary is before software event consumption, leaving USB2/UTMI RX, DWC3 link/controller state, DMA ownership, or event publication as candidates. Linux maps `-EPROTO` broadly, so this is not a direct CRC/bitstuff diagnosis and packet wrapping remains downstream | Artifact SHA `a6ec7e8bbdf5dc0534afb778ff02019aa523adf5ba4b4a7a074208f53d2ea8f9`; usbmon SHA `40dbfad20fe29e4bb8e3963d34c129f779c5fbe5e58269d02c6f7e8334b9f945`; `fastboot boot` only, no flash/erase |
| `1593531.0` | Test DWC3 USB2 transaction-level SOF liveness with a valid host-attach window (`--signal-cmd-gate sof --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:19:59`; no SOF-triggered diagnostic stop; `GET_DESCRIPTOR(Device)` submitted at `02:19:59.145484`, completed `-2` at `02:20:04.427433`, then zero-length `-71` retries at `02:20:04.784142`, `.785356`, and `.785475`; Android SuperSpeed `18d1:4ee7` recovered at `02:20:24`; no `1234` attach | Negative / the DSTS SOF-frame latch did not trigger in the valid window. Alongside the negative event-cut and setup-cut runs, the host sees the HS pull-up but the DWC3-side transaction/event path is still not proven alive. This points the next work toward UTMI/USB2 link-FSM, DWC3 receive state, or event publication—not response-payload wrapping; `-71` remains an ambiguous host error | Artifact SHA `3402bff5cdb156814b931c723775e2f4b9c4ee4496ebde4132ad534767073ed0`; usbmon SHA `c8801db3e1e1ad33905bb9edcf595a482f868231585ad275041e1c269f1c8e87`; `fastboot boot` only, no flash/erase |
| `1605252.0` | Read the retained DWC3 link state before the next attach (`--signal-cmd-gate utmi-link --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:27:43`; `GET_DESCRIPTOR(Device)` submitted at `02:27:43.416461`, completed `-2` at `02:27:48.810440`, then zero-length `-71` retries at `02:27:49.054027`, `.054155`, and `.054282`; Android SuperSpeed `18d1:4ee7` recovered at `02:28:09`; no `1234` attach | Negative / the previous boot's retained `DSTS.USBLNKST` readout selected timing bucket `0`, corresponding to `U0` in the captured snapshot; the current boot still had no setup-cut/event-cut/SOF boundary and usbmon captured no response bytes. Thus the controller can report U0 while the USB2 transaction/event path remains silent. The timing channel is a bucketed readout, so it is evidence for the retained snapshot, not a wire-level proof of packet integrity; packet wrapping remains downstream | Artifact SHA `a4a5a8675faf71d16649069b93db4dc456a43d134a09c536821fc011baed7c58`; usbmon SHA `ecea7823997c0bb10c464b0200ec786efea4b9d59a1715b4f2509e3f71ea4009`; `fastboot boot` only, no flash/erase |
| `1613408.0` | Read the retained DWC3 halt state before the next attach (`--signal-cmd-gate utmi-halt --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:32:55`; `GET_DESCRIPTOR(Device)` submitted at `02:32:55.248387`, completed `-2` at `02:33:00.618454`, then zero-length `-71` retries at `02:33:00.885396`, `.885514`, and `.885734`; Android SuperSpeed `18d1:4ee7` recovered at `02:33:21`; no `1234` attach | Negative / the previous boot's retained `DSTS.DEVCTRLHLT` readout selected timing bucket `0`, consistent with the controller not being halted. Together with `1605252.0`, the retained state is U0/nonhalt while the current USB2 transaction/event path still produces no SETUP or response data. This is a bucketed register readout, not a wire-level packet-integrity result; packet wrapping remains downstream | Artifact SHA `8525d491580e9ab3f91eebef8b241f778302d34ddd7d95281dbd7551d7c7c176`; usbmon SHA `3710f0c090d564c1e2323e752b79bc75e80c7e1b90c8eba10c7c7a53f66c438b`; `fastboot boot` only, no flash/erase |

| `1642470.0` | Re-apply the DWC3 USB2 interface contract after HS PHY initialization and read stage 3 (`--signal-cmd-gate utmi-trdtim-stage3 --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:49:07`; `GET_DESCRIPTOR(Device)` submitted at `02:49:07.852375`, completed `-2` at `02:49:12.906508`, then zero-length `-71` retries at `02:49:13.488603`, `.488741`, and `.489011`; Android SuperSpeed `18d1:4ee7` recovered at `02:49:33`; no `1234` attach | Negative / re-applying `GUSB2PHYCFG` after external HS PHY init did not move the boundary: the retained stage-3 `USBTRDTIM` readout still selected bucket `0` instead of the source-intended `9`, and usbmon still contained no response bytes. This points to the write/readback or register-adoption path, not descriptor payload wrapping | Artifact SHA `32a2c2a2883ea4a223e65a5c5dadc9e8e97a05dc1abc494e825453e0c2922bbc`; usbmon `tmp/fullerene-bramble-loop.1642470.0/usbmon-bus1.bin` SHA `eb4c131e1cf27e3e68ee0210e63409921b19b819edabb0127daa5e87d175767a`; `fastboot boot` only, no flash/erase |
| `1646394.0` | Add a 20 ms settle delay before the USB2 interface contract and repeat stage 3 (`--clock-stable-delay-us 20000 --signal-cmd-gate utmi-trdtim-stage3 --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:51:13`; `GET_DESCRIPTOR(Device)` submitted at `02:51:13.003497`, completed `-2` at `02:51:18.346583`, then zero-length `-71` retries at `02:51:18.641332`, `.644498`, and `.644677`; Android SuperSpeed `18d1:4ee7` recovered at `02:51:39`; no `1234` attach | Negative A/B / a 20 ms post-clock-enable settle did not change HS attach, the zero-length `-2`/`-71` sequence, or the stage-3 bucket-0 readout. A PLL/RCG settling delay alone is not sufficient; the next check is the DWC3 USB2 register write/adoption boundary | Artifact SHA `9e17c655f47ea44aa236fdff72a05ffeff76ea1cd2f8da761a4216c24f52713d`; usbmon `tmp/fullerene-bramble-loop.1646394.0/usbmon-bus1.bin` SHA `925f5d028a9475d2c30c6a7bc80f7b3b0491bb090c531d807974006c7a1cbd9f`; `fastboot boot` only, no flash/erase |
| `1649178.0` | Read the DWC3 USB2 interface contract immediately before Run/Stop (`--signal-cmd-gate utmi-trdtim-stage4 --observe-secs 20`) | Fullerene HS attach on bus 1 at `02:52:34`; `GET_DESCRIPTOR(Device)` submitted at `02:52:34.642378`, completed `-2` at `02:52:39.754464`, then zero-length `-71` retries at `02:52:40.281777`, `.281964`, and `.282096`; Android SuperSpeed `18d1:4ee7` recovered at `02:53:01`; no `1234` attach | Negative / the stage-4 readout also selected `USBTRDTIM` bucket `0`, so the post-HS-PHY re-apply was not visible at the pre-Run/Stop boundary. The next diagnostic is one guarded re-apply immediately after Run/Stop, with the EP0 TRB and response bytes unchanged | Artifact SHA `04c4a12c4cd4ddc9992dd3ca0ec59ab2ec82abadca0e90bd1edbdf54a22019b3`; usbmon `tmp/fullerene-bramble-loop.1649178.0/usbmon-bus1.bin` SHA `51c39bc25df9bf0c2d7b513992a9398add642dbff0d2e26265f91439be9b7de2`; `fastboot boot` only, no flash/erase |
| `1686755.0` | Read the immediate post-write `GUSB2PHYCFG` `USBTRDTIM` readback (`FULLERENE_AARCH64_USB_UTMI_REAPPLY_AFTER_RUNSTOP=1`, `--signal-cmd-gate utmi-write-readback-trdtim --observe-secs 20`) | Fullerene HS attach on bus 1 at `03:17:39`; `GET_DESCRIPTOR(Device)` reached the same zero-data `-2`/`-71` boundary at `03:17:44`/`03:17:44.781431`–`.781789`; Android SuperSpeed `18d1:4ee7` recovered at `03:18:05`; no `1234` attach | Negative / the readback timing bucket was about one step above the preceding stage-5 bucket (`42 s` versus `37 s` return), consistent with a non-zero immediate readback but not by itself a raw-register proof. The host still received no descriptor bytes, so Protocol Error remains upstream of payload wrapping; the paired requested-value run is required to distinguish an accepted write from a readback alias/retained-state artifact | Artifact SHA `954598383cf11163f4e2dae1511bacb555af06dc934216ae7017f1c8e248e66d`; usbmon `/tmp/fullerene-usbmon-utmi-write.kxIAaw/usbmon.bin` SHA `34bb3b081bff0ba7555f7b4641c0f529eab9acfca457d48075f2f295478768d9`; `fastboot boot` only, no flash/erase |
| `1691255.0` | Read the immediate `GUSB2PHYCFG` write request (`FULLERENE_AARCH64_USB_UTMI_REAPPLY_AFTER_RUNSTOP=1`, `--signal-cmd-gate utmi-write-requested-trdtim --observe-secs 20`) | Fullerene HS attach on bus 1 at `03:20:21`; `GET_DESCRIPTOR(Device)` reached the same zero-data `-2`/`-71` boundary at `03:20:26`/`03:20:27.435180`–`.435506`; Android SuperSpeed `18d1:4ee7` recovered at `03:20:50`; no `1234` attach | Negative / the requested-value selector also failed to produce `1234`; the source-selected 8-bit contract requests `USBTRDTIM=9`, but the host-visible reset bucket was cut by the same recovery/watchdog timing and cannot be used as a raw-value proof. usbmon again shows no descriptor bytes, so this Protocol Error is still upstream of response-data wrapping; the immediate readback remains the decisive measurement to make reliable | Artifact SHA `45a652dc6c9515590f564d020eca67b7d9bdd20004f6eafa07aee1bf78d91833`; usbmon `/tmp/fullerene-usbmon-utmi-requested.u4cBUt/usbmon.bin` SHA `58c588a4e29a53dce3cd5c86d7d02481f4e1291eda2b1484cfe1d0376b9f27b3`; `fastboot boot` only, no flash/erase |
| `1706004.0` | Read stage5 `GUSB2PHYCFG` after Run/Stop without the guarded post-Run/Stop re-apply (`FULLERENE_AARCH64_USB_UTMI_POSTRUN_READOUT=utmi-trdtim-stage5`, `--signal-cmd-gate utmi-trdtim-stage5 --observe-secs 20`) | Fullerene HS attach on bus 1 at `03:29:08`; `GET_DESCRIPTOR(Device)` reached the same zero-data `-2`/`-71` boundary at `03:29:13`/`03:29:14.067684`–`.067996`; Android SuperSpeed `18d1:4ee7` recovered at `03:29:34`; no `1234` attach | Negative / the pre-Run/Stop `USBTRDTIM=9` readback was not retained at stage 5: the post-Run/Stop attach returned to the baseline timing bucket (0). This identifies a Run/Stop boundary mutation or loss of the DWC3 USB2 interface contract, but it did not itself fix enumeration; usbmon still captured no descriptor bytes. The next control is re-apply after Run/Stop and read that exact post-reapply value before the host's first SETUP | Artifact SHA `f9fe5942150582659df357dcdff04e1256a71dce497fdf802ec116d5b5610dac`; usbmon `/tmp/fullerene-usbmon-utmi-postrun.TNIYQE/usbmon.bin` SHA `4fc5cfd86cd707243068c6016cd5f875af6a9123132030e3165e8e7ccfa190f8`; `fastboot boot` only, no flash/erase |
| `1713466.0` | Re-apply `GUSB2PHYCFG=9` across a halted Run/Stop transition and read post-run stage5 (`FULLERENE_AARCH64_USB_UTMI_REAPPLY_AFTER_RUNSTOP=1 FULLERENE_AARCH64_USB_UTMI_REAPPLY_HALTED=1 FULLERENE_AARCH64_USB_UTMI_POSTRUN_READOUT=utmi-trdtim-stage5`, `--signal-cmd-gate utmi-trdtim-stage5 --observe-secs 20`) | Fullerene HS attach on bus 1 at `03:33:04`; `GET_DESCRIPTOR(Device)` reached the same zero-data `-2`/`-71` boundary at `03:33:10`/`03:33:??`; Android SuperSpeed `18d1:4ee7` recovered at `03:33:31`; no `1234` attach | Negative / even `off→write→on` before the final Run/Stop left no post-run stage5 delay, so the final Run/Stop transition still clears or hides the UTMI timing field. The host again received no descriptor bytes; changing this register alone did not repair the Protocol Error path | Artifact SHA `0533fab21fc33169fc041bb447255ae81265ddc5e981aa754073ad02442e9676`; usbmon `/tmp/fullerene-usbmon-utmi-halted.Se2jZX/usbmon.bin` SHA `3d4d5f28b977760cd7f6971a2096afea04eedae76b2347461c901698c1b06dbe`; `fastboot boot` only, no flash/erase |
| `1722896.0` | Write `GUSB2PHYCFG=9` immediately after the DCTL Run/Stop write and read post-run stage5 (`FULLERENE_AARCH64_USB_UTMI_WRITE_AFTER_DCTL=1 FULLERENE_AARCH64_USB_UTMI_POSTRUN_READOUT=utmi-trdtim-stage5`, `--signal-cmd-gate utmi-trdtim-stage5 --observe-secs 20`) | Fullerene disconnected at `03:39:11` and re-attached HS at `03:39:25`; `GET_DESCRIPTOR(Device)` submitted at `03:39:25.786405`, first completion `-2` at `03:39:31.146579`, then zero-length `-71` retries at `03:39:31.426000`, `.426133`, and `.426252`; Android SuperSpeed `18d1:4ee7` recovered at `03:39:53`; no `1234` attach | Negative / the post-DCTL write did not add the expected 9-second stage5 delay: the attach latency stayed at the ~14-second baseline, so the Run/Stop transition still clears or hides the USB2 timing field even when the write lands immediately after DCTL. Every failing descriptor completion had `len=0`/`cap=0`; the host received no payload to wrap, and the Protocol Error boundary remains upstream of descriptor data | Artifact SHA `2391f52f13c7e7dbe9ad29ded04c2068991b646d932b792a5b4bbfbecbddad7e`; usbmon `/tmp/fullerene-usbmon-utmi-after-dctl.oLYhVf/usbmon.bin` SHA `c131df7f896e5fac4bf5fce4ec4b3c4e629cc74261ee217879763e8829ad80c0`; `fastboot boot` only, no flash/erase |
| `1753669.0` | Capture a gate-time DWC3 protocol-boundary snapshot and publish `dwc3-state` by same-boot attach cycles (`--signal-cmd-gate dwc3-state --observe-secs 20`) | Fullerene HS attach at `04:00:30`; `GET_DESCRIPTOR(Device)` submitted at `04:00:30.108478`, first usbmon timeout `-2` at `04:00:35.274504`, then zero-length `-71` retries at `04:00:35.745632`, `.745873`, and `.746031`; Android SuperSpeed `18d1:4ee7` recovered at `04:00:56`; no additional HS attach cycles and no `1234` attach | Negative / the new snapshot was reached, but its four-bit `dwc3-state` code was `0`: at gate evaluation there was no pending event FIFO count, no nonzero SETUP buffer, no EP0 OUT SETUP TRB HWO, and no latched DWC3 device-error event. This is a gate-time state, not proof that no earlier event existed; the host still received no descriptor bytes, so wrapping remains downstream. Individual raw `DSTS`, `DEPCMD`, `DALEPENA`, and TRB fields are the next readout split | Artifact SHA `3fa6944a2de215437362be44c73c8fe8c63efd4b10fc772676b09bbf617cf06c`; usbmon `/tmp/fullerene-usbmon-dwc3.3tzW1F/usbmon.bin` SHA `dfb121b65ad755b1f00d92adbd40447c9a93e45768b481c1182e8ceb04b698bf`; `fastboot boot` only, no flash/erase |
| `1764675.0` | Split the DWC3 snapshot to read `DALEPENA` at the first descriptor boundary (`--signal-cmd-gate dwc3-dale --observe-secs 20`) | Fullerene HS attach at `04:07:29`; `GET_DESCRIPTOR(Device)` submitted at `04:07:29.031537`, first completion `-2` at `04:07:34.090518`, then zero-length `-71` retries at `04:07:34.669784`, `.669927`, and `.670053`; Android SuperSpeed `18d1:4ee7` recovered at `04:07:55`; no additional HS attach cycles and no `1234` attach | Negative / the gate-time `DALEPENA[3:0]` readout was `0`, matching `dwc3-state=0` and confirming that the EP0 endpoint-enable mask is not present at evaluation. This does not yet prove when it was cleared, because the read is after the host abandoned the descriptor and after the poll/reset path had run; the next split is EP0 OUT/IN `DEPCMD` status plus earlier enable readbacks | Artifact SHA `3e4389ac79b309fcb556305a48f3609bbb268c8e40f3cb9f62dd4dc8d20ebffc`; usbmon `/tmp/fullerene-usbmon-dwc3-dale.QOV8Lc/usbmon.bin` SHA `87872f2b969afb7da54773a968a935a3f280c63c8d956d33d0dc8bcfc2e4f9c7`; `fastboot boot` only, no flash/erase |
| `1769692.0` | Re-apply `DALEPENA=0b11` immediately after the DCTL Run/Stop write (`FULLERENE_AARCH64_USB_DALEPENA_AFTER_DCTL=1`, `--signal-cmd-gate dwc3-dale-after-dctl --observe-secs 20`) | Fullerene HS attach at `04:10:32`; `GET_DESCRIPTOR(Device)` reached the first timeout at `04:10:37`; Android SuperSpeed `18d1:4ee7` recovered at `04:10:58`; no additional HS attach cycles and no `1234` attach | Negative / the immediate post-DCTL `DALEPENA[3:0]` readback was still `0`, so the enable mask was not observable even when rewritten at the Run/Stop boundary. This A/B did not change the zero-data enumeration failure and does not support packet wrapping as a remedy | Artifact SHA `5bbc2572bd78aae7173d0bae0b6f95bb7673e53409804b3042d914541a764d12`; usbmon `/tmp/fullerene-usbmon-dwc3-dale-reapply.k0kYuK/usbmon.bin` SHA `e4aa83ba6dfd8f812de607fe6f0b9976080cf163ce707c61efd1e5005e18c1da`; `fastboot boot` only, no flash/erase |
| `1775927.0` | Read `DALEPENA` immediately before the final DCTL Run/Stop write (`--signal-cmd-gate dwc3-dale-before-dctl --observe-secs 20`) | Fullerene HS attach at `04:14:20`; `GET_DESCRIPTOR(Device)` submitted at `04:14:20.896497`, first completion `-2` at `04:14:26.250420`, then zero-length `-71` retries at `04:14:26.535079`, `.535326`, and `.535437`; Android SuperSpeed `18d1:4ee7` recovered at `04:14:46`; no additional HS attach cycles and no `1234` attach | Negative / `DALEPENA[3:0]` was already `0` immediately before Run/Stop, after both EP0 configuration/resource pairs and the pre-connect publication writes. The enable mask is therefore absent before the final DCTL transition, and the Protocol Error cluster is upstream of any response payload; next is the command-completion/publication boundary | Artifact SHA `5b30ce6c9750cd0e420a4544d1678df19d03708f7ea84b7b794558e2fd2dfd67`; usbmon `/tmp/fullerene-usbmon-dwc3-dale-before.7FvVyu/usbmon.bin` SHA `062b600b1acb0feaae9ea15e799b32c77a398352bd7a1c9a723d657847e7c1cb`; `fastboot boot` only, no flash/erase |
| `1785948.0` | Read `DALEPENA` immediately after the EP0 OUT and EP0 IN publication writes (`--signal-cmd-gate dwc3-dale-config --observe-secs 20`) | Fullerene HS attach at `04:21:30`; the kernel journal recorded `device descriptor read/64, error -110` at `04:21:35`; Android SuperSpeed `18d1:4ee7` recovered at `04:21:57`; no additional HS attach cycles and no `1234` attach | Negative / the same-boot packed readout was `0x0`: OUT-after-write=`0`, IN-after-write=`0`. Thus the zero `DALEPENA` seen before Run/Stop is not merely a later transition-time clear; neither per-direction publication was observable immediately after its write. This run had no separate usbmon capture, so the kernel-visible symptom is `-110`; the earlier usbmon runs establish that the eventual failing control path carries no descriptor bytes | Artifact SHA `4c0f280c70508bca0ef5818c03348a7a128dbcd9d2310b632bda4a2e288a7055`; `fastboot boot` only, no flash/erase |
| `1790449.0` | Read the gate-time EP0 OUT `DEPCMD` status (`--signal-cmd-gate dwc3-cmd0 --observe-secs 20`) | Fullerene HS attach at `04:24:23`; the kernel journal recorded `device descriptor read/64, error -110` at `04:24:28`; Android SuperSpeed `18d1:4ee7` recovered at `04:24:49`; no additional HS attach cycles and no `1234` attach | Negative / the raw EP0 OUT `DEPCMD` status nibble was `0` at the DWC3 boundary snapshot, so the endpoint command engine was not left in a non-zero completion state. Together with `1785948.0`, this moves the failure below command completion and into endpoint-active publication, link transaction, or DMA/event ownership. No separate usbmon capture was taken for this run | Artifact SHA `4affb9451bf477eddd42524980799ef4979cc49586ef90eb1b17386c092c87e6a`; `fastboot boot` only, no flash/erase |
| `1792694.0` | Read the gate-time EP0 IN `DEPCMD` status (`--signal-cmd-gate dwc3-cmd1 --observe-secs 20`) | Fullerene HS attach at `04:25:33`; the kernel journal recorded `device descriptor read/64, error -110` at `04:25:38`; Android SuperSpeed `18d1:4ee7` recovered at `04:25:58`; no additional HS attach cycles and no `1234` attach | Negative / the raw EP0 IN `DEPCMD` status nibble was also `0`. Both EP0 direction commands therefore retire as status-zero while the per-direction `DALEPENA` readbacks remain zero immediately after publication. This leaves the active-endpoint register boundary or a later endpoint/link ownership transition ahead of packet payload; wrapping is still downstream | Artifact SHA `559d95cfdca4bdf649623796792e5060163809bcd3428f1fa4cbb2f00170498a`; `fastboot boot` only, no flash/erase |
| `1820841.0` | Run the post-Run/Stop event-DMA probe while retaining the valid event-consumer gate (`--signal-dma-post-runstop --signal-cmd-gate event-cut --observe-secs 20`) | Fastboot disconnected at `04:44:10`; Fullerene HS attach at `04:44:25`; `device descriptor read/64, error -110` at `04:44:30`; Android SuperSpeed `18d1:4ee7` recovered at `04:44:51`; no `1234` attach or diagnostic disconnect | Negative boundary readout / no consumed DWC3 USB-reset or event reached the `event-cut` latch. The post probe was enabled, but this gate did not publish its START/END/event-DMA result, so no probe-bit claim is made; the host-visible failure remains the same pre-payload boundary | Artifact SHA `5c218ef8b3504330b24bacb4e6e0b130eedcbb8f51777c818424391014e8e97f`; `fastboot boot` only, no flash/erase |
| `1825284.0` | Directly gate on the complete post-Run/Stop event-DMA result (`--signal-dma-post-runstop --signal-cmd-gate post --observe-secs 20`) | Fastboot disconnected at `04:46:37`; Fullerene HS attach at `04:46:51`; `device descriptor read/64, error -110` at `04:46:57`; Android SuperSpeed `18d1:4ee7` recovered at `04:47:17`; no `1234` attach or diagnostic disconnect | Negative / the composite `post` condition did not produce a host-visible positive readout. This still conflates “probe record absent” with an incomplete START/END/event-DMA bitmask, so the next control adds an explicit bitmask/record readout; no descriptor payload was available to wrap | Artifact SHA `b9782aad6ae45203b6a96bd463dfa30959562e29c22734460523fad31a709b79`; `fastboot boot` only, no flash/erase |
| `1827879.0` | Gate on whether the post-Run/Stop probe emitted any trace record (`--signal-dma-post-runstop --signal-cmd-gate post-record --observe-secs 20`) | Fastboot disconnected at `04:47:58`; Fullerene HS attach at `04:48:12`; `device descriptor read/64, error -110` at `04:48:17`; Android SuperSpeed `18d1:4ee7` recovered at `04:48:38`; no `1234` attach or diagnostic disconnect | Negative / the record-presence gate also produced no host-visible positive result. A boolean gate cannot distinguish “post probe not reached” from a recorded probe with failed bits, so this run is superseded by the numeric `post-code` readout; the `-110` boundary and no-payload observation remain unchanged | Artifact SHA `778e7b00e1931367ba561e8716ca10e76b98cca2ec96172459c50b91b16f7eea`; `fastboot boot` only, no flash/erase |
| `1834774.0` | Encode the post-Run/Stop event-DMA result as a numeric same-boot readout (`--signal-dma-post-runstop --signal-cmd-gate post-code --observe-secs 20`) | Fastboot disconnected at `04:52:52`; Fullerene HS attach at `04:53:07`; `device descriptor read/64, error -110` at `04:53:12`; Android SuperSpeed `18d1:4ee7` recovered at `04:53:32`; no extra HS attach cycles and no `1234` attach | Negative / the `post-code` path produced no host-visible cycle count. It did not yet separate “no retained probe record” from failed probe bits because the stop/run channel itself was not observable; no descriptor payload was available to wrap | Artifact SHA `c540b70d4704ef28ebf258f7f14405be564bf09e05cd887e328a04c4b8a50385`; `fastboot boot` only, no flash/erase |
| `1838141.0` | Repeat the numeric post-Run/Stop event-DMA readout with a shorter observation window (`--signal-dma-post-runstop --signal-cmd-gate post-code --observe-secs 15`) | Fastboot disconnected at `04:54:43`; Fullerene HS attach at `04:54:57`; `device descriptor read/64, error -110` at `04:55:02`; Android SuperSpeed `18d1:4ee7` recovered at `04:55:23`; no extra HS attach cycles and no `1234` attach | Negative / shortening the gate window did not make the post-code channel observable. The result remains a transport/readout failure, not evidence that any individual START/END/event-DMA bit was clear; the host-visible failure remains pre-payload | Artifact SHA `dd17aca39d73379405d4a96e823f1dd5c9490300981e7ec3038da5d5ba8d9870`; `fastboot boot` only, no flash/erase |
| `1844635.0` | Remove the stale `DSTS.DEVCTRLHLT` suppression from the post-Run/Stop diagnostic probe (`--signal-dma-post-runstop --signal-cmd-gate post-record --observe-secs 15`) | Fastboot disconnected at `04:58:44`; Fullerene HS attach at `04:58:58`; `device descriptor read/64, error -110` at `04:59:03`; Android SuperSpeed `18d1:4ee7` recovered at `04:59:24`; no extra HS attach cycles and no `1234` attach | Negative / allowing the post probe whenever DCTL Run/Stop remained set still produced no host-visible record. This does not prove that the probe did not execute; the attach-cycle channel is inadequate for this boundary, while the no-payload Protocol Error boundary is unchanged | Artifact SHA `3e2294c367e57dc76028cbdec899361358e3a430fb0909f4dbf905b4726b79db`; `fastboot boot` only, no flash/erase |
| `1854468.0` | Try the `protocol` selector against the retained EP0 STARTTRANSFER/SETUP classifier (`--signal-probe --signal-cmd-gate protocol --observe-secs 20`) | Fastboot disconnected at `05:05:10`; Fullerene HS attach at `05:05:25`; `device descriptor read/64, error -110` at `05:05:30`; Android SuperSpeed `18d1:4ee7` recovered at `05:05:51`; no extra HS attach cycles and no `1234` attach | Inconclusive / the generic UTMI selector only stopped the device and parked for a code-dependent delay; it did not publish the protocol code. The source classifier is now wired to a dedicated same-boot numeric attach-cycle path, with no PHY/TRB/response-data change | Artifact SHA `8be691d53662ebcdd2e5103a58719f90618b6cd3180280c46468f2bdf7038d8e`; no usbmon artifact; `fastboot boot` only, no flash/erase |
| `1867207.0` | Run the dedicated same-boot numeric `protocol` readout (`--signal-probe --signal-cmd-gate protocol --observe-secs 20`) | Fastboot disconnected at `05:14:06`; Fullerene HS attach at `05:14:21`; `device descriptor read/64, error -110` at `05:14:26`; Android SuperSpeed `18d1:4ee7` recovered at `05:14:47`; no extra HS attach cycles and no `1234` attach | Negative readout channel / the dedicated path also produced no host-visible attach-cycle count. This must not be interpreted as protocol code `0`: DCTL stop/run is not a reliable readout on this board. The host still received no descriptor payload; `-71` remains a separate host completion class from this run's `-110` timeout | Artifact SHA `866c24ef06456e7c5812a9c4391940b70431ef27dfc72f4c09806d17cd6cbd17`; no usbmon artifact; `fastboot boot` only, no flash/erase |
| `1873672.0` | Reproduce the source-defined 16-bit UTMI width with the deliberately mismatched `USBTRDTIM=9` (`FULLERENE_AARCH64_USB_PHYIF_16BIT=1 FULLERENE_AARCH64_USB_USBTRDTIM=9`, plus `--signal-cmd-gate protocol`) | Fullerene HS attach at `05:17:59`; usbmon recorded `GET_DESCRIPTOR(Device)` completions with `status=-71` at `05:17:59.884583`, `.884744`, and `.884876`; all had `len=0/cap=0`; Android SuperSpeed `18d1:4ee7` recovered at `05:18:25`; no `1234` attach | Negative / the width/timing mismatch fails at the first control transaction, before any descriptor payload reaches xHCI. This is a transport/framing boundary, not a data-wrapping problem; the run also confirms that the same-boot protocol readout remains silent when DCTL stop/run is not host-visible | Artifact SHA `6733ab8d75548dd70e6237d7c4dc04b6c001bf9a013c47217effd40425fe4008`; usbmon `/tmp/fullerene-usbmon-protocol.c9hC5n/usbmon.bin` SHA `de449c3154909d75616cca6c2ebe03f5fde23a5498f86c097a45ea40c2c89ca2`; `fastboot boot` only, no flash/erase |
| `1878313.0` | Hold UTMI at 8-bit and change only `USBTRDTIM` from the source-defined `9` to `5` (`FULLERENE_AARCH64_USB_USBTRDTIM=5`, plus `--signal-cmd-gate protocol`) | Fullerene HS attach at `05:20:42`; usbmon completed the first `GET_DESCRIPTOR(Device)` with `-2` at `05:20:47.562440`, then three zero-length `-71` completions at `05:20:47.967828`, `.967967`, and `.968097`; Android SuperSpeed `18d1:4ee7` recovered at `05:21:08`; no `1234` attach | Negative / TRDTIM alone changes the timing of the host error but does not make a response valid. Together with the source-correct 16-bit+5 result, the dominant split is PHYIF width/PHY contract rather than descriptor wrapping; the host still received no payload | Artifact SHA `5967d75142467d3fab53c018a4215eba30e39d9f0155f1f6022967e0f12ba11d`; usbmon `/tmp/fullerene-usbmon-protocol-8bit5.1EUpTO/usbmon.bin` SHA `6b11343b5928077238edf79edc76e6713c6434c2cef1f85b036decbe9219f49`; `fastboot boot` only, no flash/erase |
| `Correction 2026-09-02` | Reclassify the `1566338.0` setup-cut and `1571023.0` event-ring-size runs | Measurement correction | Both used `--observe-secs 1`, which ended before the measured ~14-second Fastboot-disconnect-to-HS-attach delay. Their missing setup-cut disconnects do not classify SETUP/event delivery; the usbmon zero-data observations remain valid. |
| `Source correction 2026-09-02` | Correct direct-handoff mock-UTMI source selection from the official Lito GCC table | Source evidence | `gcc_usb30_prim_mock_utmi_clk_src` exposes 60 MHz (`GPLL0_OUT_EVEN / 5`); 19.2 MHz is the RPMh `ref_clk_src` for the QUSB2 HS PHY. The former 19.2 MHz direct default is now retained only as an explicit negative-control flag. |
| `1901078.0` | Re-run the current direct baseline with source-correct 60 MHz mock UTMI, 8-bit UTMI/`USBTRDTIM=9`, and a 4096-byte Android-sized event ring (`--event-ring-size-4096 --signal-cmd-gate protocol --observe-secs 20`) | Fullerene HS attach at `05:35:42`; the first `GET_DESCRIPTOR(Device)` was submitted at `05:35:42.003405`, completed with `-2` at `05:35:47.146418`, then three zero-length `-71` completions at `05:35:47.642918`, `.643068`, and `.643162`; Android SuperSpeed `18d1:4ee7` recovered at `05:36:08`; no `1234` attach | Negative / 60 MHz plus the 4096-byte event ring did not produce descriptor data or change the `-2`→`-71` boundary. The host received `len=0`/`cap=0` for every failing descriptor completion, so response wrapping remains downstream; the event-ring-size A/B is negative under the source-correct clock, but this does not yet prove the DWC3 producer/event path was consumed | Artifact SHA `9338bf10cc446000ab66518c88a2132d3814674dfc3b5e73f71153ead3a2a5d9`; usbmon `/tmp/fullerene-usbmon-mock-utmi60-ev4096.3xcily/usbmon.bin` SHA `353338ca817e9f7e82ad765e2fb698e9ea23d1b4b7767b8f3727ebf1522911a8`; `fastboot boot` only, no flash/erase |
| `1910386.0` | Add `--start-ungated` to the source-correct 60 MHz + 4096-byte event-ring baseline, allowing EP0 `STARTTRANSFER` despite the live `DSTS.DEVCTRLHLT` readback | Fullerene HS attach at `05:41:26`; the first `GET_DESCRIPTOR(Device)` was submitted at `05:41:26.934457`, completed with `-2` at `05:41:32.235459`, then three zero-length `-71` completions at `05:41:32.570878`, `.570997`, and `.571117`; Android SuperSpeed `18d1:4ee7` recovered at `05:41:53`; no `1234` attach | Negative / removing the HALT readback gate did not change the zero-data `-2`→`-71` boundary. The command-result path alone is therefore insufficient; the remaining discriminator is whether DWC3 generated an event/DMA write that Fullerene failed to consume, or never generated one because USB2 RX/UTMI framing stopped below software | Artifact SHA `8f82e7115cefe484d3a042a9bc5c9cc1bec1b63aaae8447624dc2625cd68266b`; usbmon `/tmp/fullerene-usbmon-mock-utmi60-ev4096-ungated.sUD1ke/usbmon.bin` SHA `c7bd85047c8f3f406889d346c34e7f36deb4d538786e94f0f151d111f1927dfd`; `fastboot boot` only, no flash/erase |
| `1915458.0` | Reproduce Android msm's `dwc3_gadget_restart()` immediately before Run/Stop (`--gadget-restart-at-runstop`) on the source-correct 60 MHz + 4096-byte event-ring path | Fastboot disconnected at `05:44:15`; no Fullerene HS attach followed; Android SuperSpeed `18d1:4ee7` recovered at `05:44:55`; usbmon was empty and no `1234` attach occurred | Negative / the full Android restart sequence could not be inserted after the existing EP0 resource epoch: it suppressed the HS attach before the first host control request. This is not evidence that Android's sequence is wrong in its native lifecycle; it shows that a post-config restart must first revoke/rebuild ownership atomically, so it is not a valid payload or wrapping fix for the current handoff | Artifact SHA `d0e46fd3c6cf170465f5330649be06550958c513a7d3c6f1b014de78efc594a8`; usbmon `/tmp/fullerene-usbmon-mock-utmi60-ev4096-restart.Kw3ulr/usbmon.bin` SHA `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; `fastboot boot` only, no flash/erase |
| `1923127.0` | Reuse Fastboot's captured EP0 event-DMA base on the source-correct 60 MHz direct-handoff path (`--reuse-fastboot-dma --start-after-connect --signal-cmd-gate protocol --observe-secs 20`) | Fastboot disconnected at `05:49:44`; Fullerene HS attach at `05:49:58`; the host reported `device descriptor read/64, error -110` at `05:50:04`; Android SuperSpeed `18d1:4ee7` (`idVendor=18d1,idProduct=4ee7`) recovered at `05:50:24`; no `1234` attach | Negative DMA-ownership A/B / reusing the firmware-owned event-buffer address did not move the first control-transfer boundary or produce a descriptor. This run had no usbmon capture because the capture helper lacked the `usbmon` group in its subprocess; therefore no new `-71`/payload classification is claimed. It does show that the linker-owned event page versus Fastboot's captured event page is not sufficient by itself; packet wrapping remains downstream | Artifact SHA `8086254498650bf3f0bff3de51ce98ad701d2c8d302df2610f77dd63bcda508a`; host log `tmp/fullerene-bramble-loop.1923127.0/kernel-final.log`; `fastboot boot` only, no flash/erase |
| `1928181.0` | Re-publish the EP0 event ring immediately before Run/Stop on the source-correct 60 MHz path (`--event-ring-at-runstop --start-ungated --event-ring-size-4096 --signal-cmd-gate protocol --observe-secs 20`) | Fullerene HS attach at `05:52:55`; usbmon captured the first `GET_DESCRIPTOR(Device)` submit at `05:53:00.579427`, completion `-2` at `05:53:00.800778`, then three zero-length `-71` completions at `.800931`, `.801136`, and `.801370`; Android SuperSpeed `18d1:4ee7` recovered at `05:53:21`; no `1234` attach | Negative event-publication A/B / moving the ring publication to the Run/Stop boundary and removing the HALT gate did not change the zero-data `-2`→`-71` shape. The host still received no descriptor payload, so wrapping remains downstream; the event producer/EP0 ownership boundary is still not proven | Artifact SHA `efcf1e3435163af74e095c82546ef2e0a80e61ec941897102a88cfeed396d9ba`; usbmon `/tmp/fullerene-usbmon-event-ring-runstop.vYX289/usbmon.bin` SHA `17c07092898e17dbf846eb9ced461340189c3f14681bcdc84b3c3fdf4118c681`; `fastboot boot` only, no flash/erase |
| `1935622.0` | Force DWC_usb31 `GUCTL3.USB20_RETRY_DISABLE=1` while retaining the source-correct 60 MHz + 8-bit/`USBTRDTIM=9` + 4096-byte path (`FULLERENE_AARCH64_USB_GUCTL3_USB20_RETRY_SET=1 --signal-cmd-gate protocol --observe-secs 20`) | Fullerene HS attach at `05:57:20`; usbmon captured the first `GET_DESCRIPTOR(Device)` submit at `05:57:20.986371`, completion `-2` at `05:57:26.090482`, then three zero-length `-71` completions at `05:57:26.624196`, `.624306`, and `.624455`; Android SuperSpeed `18d1:4ee7` recovered at `05:57:47`; no `1234` attach | Negative USB2 retry-policy A/B / forcing the DWC_usb31 retry workaround did not alter the `-2`→`-71` boundary or create a payload. This excludes that retry-policy bit as the immediate fix for the current Protocol Error path; no packet wrapping was involved | Artifact SHA `e2ab4374f5a1f050616ff2df6d557fe7820aefbdf5900dd897d7dd2bfd2ba1d8`; usbmon `/tmp/fullerene-usbmon-guctl3-retry-set.r9QenF/usbmon.bin` SHA `b80a3cba64aeb0be34ad73903c687d2183e9c6f7e7ffcaa04fc5d729d67a5b6d`; `fastboot boot` only, no flash/erase |
| `1962618.0` | Capture the first DWC3 event-buffer producer boundary while classifying the host Protocol Error (`--signal-cmd-gate dwc3-first-event --observe-secs 20`) | Fullerene HS attach at `06:14:11`; usbmon submitted `GET_DESCRIPTOR(Device)` at `06:14:11.574443`, completed `-2` at `06:14:16.778450`, then three zero-length `-71` retries at `06:14:17.210207`, `.210300`, and `.210419`; Android SuperSpeed `18d1:4ee7` recovered at `06:14:37`; no diagnostic attach-cycle readout and no `1234` attach | Negative / the host-side `-71` cluster is reproducible and carries no data. Linux/xHCI maps several completion causes (including USB transaction error, stall, and incompatible-device error) to `-EPROTO`; the exact xHCI completion code was unavailable because tracefs is root-only. The new DWC3 first-event latch did not produce a host-visible readout, so this must not be interpreted as raw code zero; packet wrapping remains downstream | Artifact SHA `1e1190716b01f9d9510180d2872dc15d76f0043b93771d574f7b8b7409944916`; usbmon `/tmp/fullerene-usbmon-dwc3-first-event.RMLzwa/usbmon.bin` SHA `f377497cefa8ee30b5e6445dffac3b4b5d5365ca135ebc705409cbafcab7b93c`; `fastboot boot` only, no flash/erase |
| `1968411.0` | Re-test XBL-observed event/setup/TRB DMA addresses with the source-correct 60 MHz mock UTMI baseline (`--xbl-event-dma --xbl-stock-ep0-dma --start-after-connect --start-ungated --android-resource-order --signal-cmd-gate dwc3-first-event --observe-secs 20`) | Fullerene HS attach at `06:18:26`; usbmon address-0 `GET_DESCRIPTOR(Device)` control transfer entered `-2` completions through `06:18:32.036440`, then three zero-length `-71` completions at `06:18:32.674943`, `.675062`, and `.675182`; Android SuperSpeed `18d1:4ee7` recovered at `06:18:51`; no `1234` attach | Negative DMA-address A/B / combining XBL's `GEVNTADR0=0x0a6fc010`, setup buffer `0x80798d70`, and first TRB `0x80798f70` did not create a descriptor payload or move the pre-response Protocol Error boundary. The result was obtained with the corrected 60 MHz mock-UTMI source, so the XBL address hypothesis is not sufficient; packet wrapping remains downstream | Artifact SHA `0c350075c90a89b6b88176a7aa2b0be25ef0f89f4d8ae4f94da19faeb23592e9`; usbmon `/tmp/fullerene-usbmon-xbl-dma-60.wtCYkC/usbmon.bin` SHA `421cbc3a4b4937dac52cc01cd0f7f828c1bfc1f421e4379297eaaeeec9fe3126`; `fastboot boot` only, no flash/erase |
| `1979839.0` | Match Android `msm_hsphy_reset()` timing: assert `GCC_QUSB2PHY_PRIM_BCR`, wait 100 µs with the architectural counter, then deassert (`--start-after-connect --start-ungated --android-resource-order --event-ring-size-4096 --signal-cmd-gate dwc3-first-event --observe-secs 20`) | Fullerene HS attach at `06:25:47`; usbmon submitted `GET_DESCRIPTOR(Device)` at `06:25:47.724413`, completed `-2` at `06:25:53.098555`, then three zero-length `-71` retries at `06:25:53.361118`, `.361218`, and `.361337`; Android SuperSpeed `18d1:4ee7` recovered; no `1234` attach | Negative timing A/B / replacing the CPU-frequency-dependent 250,000-NOP reset waits with Android's 100 µs assert interval did not move the zero-data `-2`→`-71` boundary. The Linux EP0 TRB/STARTTRANSFER form was already source-matched, and no response bytes existed to wrap; next isolate HS-PHY rail enable state before the reset/init boundary | Artifact SHA `eddb6df1c4173b942d76b4a19915db1f3ea8f066236ae4bf3f770f5cc390c0ea`; usbmon `/tmp/fullerene-usbmon-hsphy-reset.g94YdN/usbmon.bin` SHA `afb03b92acb64c4c071e4bf83314d581fcd62a7d8823c4077915e3708296817d`; `fastboot boot` only, no flash/erase |
| `1989813.0` | Re-assert Android `msm_hsphy_init()`'s QUSB2 `vdd/vdda18/vdda33` rail requests before the direct handoff reset/init (`--refresh-hsphy-power --start-after-connect --start-ungated --android-resource-order --event-ring-size-4096 --signal-cmd-gate dwc3-first-event --observe-secs 20`) | Fullerene HS attach at `06:32:46`; usbmon submitted `GET_DESCRIPTOR(Device)` at `06:32:46.416460`, completed `-2` at `06:32:51.914416`, then three zero-length `-71` retries at `06:32:52.138462`, `.138577`, and `.138697`; Android SuperSpeed `18d1:4ee7` recovered; no `1234` attach | Negative HS-PHY power A/B / re-sending the three Android-described HS-PHY rail requests did not move the zero-data `-2`→`-71` boundary. The first host transfer still had no payload, and the exact xHCI subcode/DWC3 first-event latch remained unavailable; packet wrapping is downstream | Artifact SHA `85624be95076141b5a59040c9813a9e11fe8d421a6128d5fd395b077e5fd488d`; usbmon `/tmp/fullerene-usbmon-hsphy-power.Lhg3W2/usbmon.bin` SHA `30695f2187acce7f0682cdd027aea96d3f90a34d0b58132c3ac85d6d3fbf2d8e`; `fastboot boot` only, no flash/erase |
| `1997970.0` | Restore Android/Linux's post-core-init USB2 PHY low-power state by setting `GUSB2PHYCFG.SUSPHY=1` immediately before the direct Run/Stop boundary (`--usb2-susphy --start-after-connect --start-ungated --android-resource-order --event-ring-size-4096 --signal-cmd-gate dwc3-first-event --observe-secs 20`) | Fullerene HS attach at `06:38:56`; usbmon submitted `GET_DESCRIPTOR(Device)` at `06:38:56.469464`, completed `-2` at `06:39:01.578496`, then three zero-length `-71` retries at `06:39:01.794353`, `.794444`, and `.794563`; Android SuperSpeed `18d1:4ee7` recovered; no `1234` attach | Negative USB2 SUSPHY A/B / enabling `SUSPHY` before Linux's Run/Stop-style guard did not move the zero-data `-2`→`-71` boundary. The host's first transfer still had no returned bytes, so descriptor payload wrapping cannot affect this failure; exact xHCI subcode and DWC3 first-event latch remain unavailable | Artifact SHA `9144b3901a9fd4bf6621a391fe79c83785e71d04df3dc7cf351e7321e2c999d6`; usbmon `/tmp/fullerene-usbmon-usb2-susphy.MsMihY/usbmon.bin` SHA `8675a8cdbf1504837113395fd56f9ef737825ede0ad91ac0971fa3d2a905dc4e`; `fastboot boot` only, no flash/erase |
| `2011364.0` | Add a dedicated DWC3 protocol-boundary classifier and attempt to publish `0=no record`, `1=STARTTRANSFER timeout`, `2=No Resource`, `3=other status`, `4=success`, `5=SETUP seen` through an APSS-WDT 1–6 s bite (`--signal-cmd-gate dwc3-bite --start-after-connect --start-ungated --android-resource-order --event-ring-size-4096 --observe-secs 20`) | Fullerene HS attach at `06:48:42`; usbmon submitted `GET_DESCRIPTOR(Device)` at `06:48:42.673426`, completed `-2` at `06:48:47.820006`, then three zero-length `-71` retries at `06:48:48.310266`, `.310417`, `.310563`; Android SuperSpeed `18d1:4ee7` recovered at `06:49:10`; no `1234` attach | Negative / the host-side `-71` cluster remained zero-data. Recovery stayed in the common `fastboot`-accept-to-Android ~42 s `boot-reason=watchdog` bucket, so the APSS-WDT timing channel was not writable/observable on this production device and no DWC3 code is claimed. This confirms the classifier is diagnostic-only; packet wrapping remains downstream | Artifact SHA `d0a6b58f8ca7bb4bfc409a12760b2061be49ed062353d9eedd16ace030ede8a`; usbmon `/tmp/fullerene-usbmon-dwc3-bite.TGWIRM/usbmon.bin` SHA `89b61a8fff61a5b7e41e6393ead37a6fbc217ba81c45cf157de8a03bac31a781`; `fastboot boot` only, no flash/erase |
| `2051848.0` | Test ABL's command-kind `DEPCMDPAR` write mask (`--abl-command-params`) on the source-correct 60 MHz / 8-bit / 4096-byte attach-reaching baseline | Fullerene HS attach at `07:18:46`; the host reported `device descriptor read/64, error -110` at `07:18:52`; Android SuperSpeed `18d1:4ee7` recovered at `07:19:12`; no `1234` attach | Inconclusive negative / ABL-style parameter publication did not reach enumeration or move the kernel-visible boundary. This run had no parallel usbmon capture, so it does not classify the `-71` subcase; the A/B changed only endpoint-command publication and not PHY, TRB, descriptor, or packet bytes | Artifact SHA `1f9672fe751671fff73ad7607b7085069fd20b3e5ca6a873547590e52515ed75`; host log `/home/placeless/dev/fullerene/tmp/fullerene-bramble-loop.2051848.0/kernel-final.log`; `fastboot boot` only, no flash/erase |
| `2069143.0` | Run exact ABL/Qualcomm msm EP0 `SETEPCONFIG` fields together with ABL's command-kind parameter-write mask (`--abl-ep-config --abl-command-params --signal-cmd-gate dwc3-first-event`) | Fullerene HS attach at `07:32:00`; usbmon submitted `GET_DESCRIPTOR(Device)` at `07:32:00.840409`, completed `-2` at `07:32:06.218458`, then three zero-length `-71` completions at `07:32:06.479907`, `.480026`, and `.480145`; Android SuperSpeed `18d1:4ee7` recovered at `07:32:26`; no `1234` attach | Negative A/B / the disassembly-confirmed EP0 `P0/P1` fields (`0x00c00200`, `0x00000300/0x02000300`) plus ABL's selective parameter-write mask did not create a descriptor payload or alter the zero-data `-2` to `-71` boundary. All four descriptor completions had `len=0`, `cap=0`; wrapping is downstream. The diagnostic gate produced no host-visible latch and does not establish a raw xHCI/DWC3 completion code | Artifact SHA `cb7d9350dbaf5bc16e35a34e8afa9e0e8f830f6af320a42d1b7799c24c3bbdc0`; usbmon `/tmp/fullerene-usbmon-abl-ep-config-exact.bin` SHA `fd1bea9a1d5c3afc84c8bd7fa89f0c4a0920b1875e7145b5eeb24161cc5e2ab4`; `fastboot boot` only, no flash/erase |
| `2092314.0` | Run the source-confirmed ABL EP0 request/TRB control flags (`--abl-trb-flags`) for `CONTROL_SETUP`, `CONTROL_DATA`, and `CONTROL_STATUS` | Fullerene HS attach at `07:50:13`; usbmon submitted `GET_DESCRIPTOR(Device)` at `07:50:13.650388`, completed `-2` at `07:50:18.829306`, then three zero-length `-71` completions at `07:50:19.289165`, `.289291`, and `.289460`; no `1234` attach; Android recovery was watchdog-based | Negative A/B / using ABL's `0x405` (`HWO|CHN|ISP_IMI`) base instead of `LST|IOC` did not create a descriptor payload or change the zero-data `-2` to `-71` boundary. The optional second `0x403` TRB is not part of this first test: the initial request slot's chain flag is zero and the 64-byte response is not at the observed aligned-length branch. The result moves the suspect to the ABL request-object/ring ownership and event-consumption sequence, not packet wrapping | Artifact SHA `4e0269bc37a1c3468e831373607e53cc2240cf12d542d748e89ea819bc7f525a`; usbmon `/tmp/fullerene-usbmon-abl-trb-flags.bin` SHA `deacf5c5a76635047a895eb4166f862042276dfc33f3a9548585915bbd7943bb`; `fastboot boot` only, no flash/erase |
| `Next source A/B` | Compare ABL's request-object/ring ownership state around `0x29b14` with the local EP0 request slots and event consumer | Pending source audit | ABL's `0x405` control word was negative in isolation. Resolve the request-slot fields (`endpoint`, buffer, length, optional-chain flag), ring cursor/link state, and when ABL acknowledges/advances the event ring before adding another TRB-flag variant; retain the 60 MHz/8-bit/`USBTRDTIM=9`/4096-byte and exact EP-config baseline |
| `Next control` | Obtain the exact xHCI completion code if privileged tracefs becomes available; otherwise preserve and inspect the DWC3 first-event latch on the first run that reaches trace transport | Pending diagnostic | Keep 60 MHz, 8-bit UTMI/`USBTRDTIM=9`, 4096-byte ring, and ungated STARTTRANSFER fixed. Treat `-71` as a broad host error class, and do not alter descriptor bytes or add packet wrapping until a non-zero DWC3 event/count or a host completion subcode is observed. |

## Future Platforms

In the future, we plan to add compatibility notes for:

- **ThinkPad** series
- **Framework** laptops
- **Intel** reference platforms
- **AMD** platforms
- **QEMU** (already supported; detailed notes to be added)
