[package]
name = "carrier"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = { version = "0.10.0", default-features = false, features = ["mutex", "spin_mutex"] }

[lib]
name = "carrier"
path = "src/lib.rs"
