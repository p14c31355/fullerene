[workspace]

[package]
name = "viewer"
version = "0.1.0"
edition = "2024"

[dependencies]
image = { version = "0.25", default-features = false, features = [
  "jpeg",
  "png",
  "bmp",
  "gif",
] }

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
