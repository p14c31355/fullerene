[package]
name = "lattice"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
ab_glyph = { version = "0.2.32", default-features = false, features = ["libm"] }
spin = { workspace = true, features = ["spin_mutex", "once"] }
